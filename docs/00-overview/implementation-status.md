# Implementation Status

> **A single, living dashboard of what's actually built vs. still planned — across the roadmap, every RFC, every ADR, and every architecture document — so a new implementation session can start here instead of reconstructing it from `git log` or re-reading five RFCs.** This is a status *index*, not a source of truth: each row links to the document that actually defines the thing. Update the relevant row (and add a dated line to the changelog at the bottom) whenever a milestone ships, an RFC's scope changes, or an ADR is accepted — the failure mode this file exists to prevent is exactly what happened to the old `M0-IMPLEMENTATION-BACKLOG.md` checklist (see [archive/obsolete/M0-IMPLEMENTATION-BACKLOG.md](../archive/obsolete/M0-IMPLEMENTATION-BACKLOG.md)): left unread while the code moved on, until it actively misled.
>
> **Last full audit: 2026-07-20.** Two different axes are tracked separately per item, because they move independently in this project: **ratification** (has a human formally accepted this decision under [ADR-0000](../03-adrs/ADR-0000-governance-and-ratification-process.md)?) and **implementation** (does the code actually do this today?). A document can be fully implemented and still formally unratified — that is this project's normal state, not a defect, per [m0-plan.md](../04-guides/m0-plan.md)'s "nothing blocks writing code."

## 1. Roadmap milestones

The full milestone *plan* (themes, "usable system" definitions) lives in [roadmap.md](roadmap.md). Current shipped status:

| Milestone | Status | Notes |
|---|---|---|
| **M0** — Walking skeleton | Shipped, validated live | All of M0.0–M0.3 (`foundry do`, scripted + real Executor, budget, repair, `log`/`show`). Historical plan: [archive/obsolete/M0-IMPLEMENTATION-BACKLOG.md](../archive/obsolete/M0-IMPLEMENTATION-BACKLOG.md). Confirmed working end-to-end with a live Executor on 2026-07-18 — see §7. |
| **M1** — Deterministic core | Shipped | Verified/judged/budgeted Acts, immutable filesystem Record, replay (`foundry replay`) and resume (`foundry resume`, mid-Pipeline checkpoints). **Resume was silently dead for every interactive-session Act until 2026-07-22** (`session.NewSession` never wired `SetCheckpointSaver`, and `wireEngine` only resolved built-in Pipeline names) — fixed same day; see the hardening changelog entry below. |
| **M2** — Reusable production + mock executor | Shipped | Authored Pipeline documents (`.foundry/pipelines/*.json`) are the reusable-Act artifact, per [ADR-0004](../03-adrs/ADR-0004-reusable-act-template-format-and-evolution-policy.md) (ratified and implemented 2026-07-20); `executor.ScriptedExecutor` is the fixture Executor; bounded repair works. |
| **M3** — Real executors | Shipped, partially | Six real Executors behind a `Router` — Claude Code (default), OpenAI, Gemini CLI, Gemini API, Copilot CLI, and a general `openai-compatible` vendor (all named). **`executor/copilotcli` added 2026-07-23**, per the maintainer's own observation that Copilot could be delegated generate Steps too, not only PR review: it runs the GitHub Copilot CLI (GA since 2026-02-25) the same "delegate auth to the vendor's own CLI" way `executor/geminicli` does. Unlike Claude Code and the Gemini CLI (both already proven, live, to stay text-only when prompted this way), the Copilot CLI ships its own agentic file/shell tools — this Executor deliberately grants none of them and additionally verifies the workspace's `git status --porcelain` is unchanged before and after every call, refusing outright otherwise, so a real Accountability-axis violation (a generate Step mutating the workspace directly, bypassing Foundry's own approval gate) can never pass silently. **Not validated live**: no `copilot` CLI installed in this environment — this is the one Executor in the repo carrying a real, un-mitigated-by-testing risk (an agentic tool behaving unexpectedly under `--no-ask-user`), flagged explicitly in its own package doc and in getting-started.md rather than presented as equally proven as the others. **`openai-compatible` added 2026-07-23**, after researching further free/cheap providers the maintainer asked about: rather than one near-duplicate package per vendor, `executor/openai` gained `NewExecutorWithEndpoint(model, apiKey, endpoint)`, reused against any caller-named `base_url` — Ollama (free, local, no key), Groq, DeepSeek, and GitHub Models (reusing the same `GH_TOKEN` class `remote_publish_token_env` already needs) all document an explicit OpenAI-Chat-Completions-compatible endpoint, so one client already covers all of them. Amazon Q Developer was researched and explicitly skipped: AWS announced its end-of-support in 2026, closing new signups (including free-tier) before this work even started. **`executor/gemini` (HTTP API) added 2026-07-23**, then **`executor/geminicli` (Gemini CLI subprocess) added the same day**: per the maintainer's request to support cheap/free-tier vendors, and then explicitly to prefer a friendlier auth story over raw API keys, `"gemini"` now resolves to `executor/geminicli` — a subprocess wrapper mirroring `executor/claude` exactly, delegating auth entirely to the Gemini CLI's own cached "Sign in with Google" login (PIC-2: Foundry reads no API key) — while `"gemini-api"` names the original HTTP path explicitly, kept only as a last resort for environments with no browser to complete that one-time login. No new architectural decision needed for any of this — ADR-0005's Executor contract and ADR-0006's explicit-pin routing already cover any number of named vendors; `project.ExecutorConstructor` gained a `workspace string` parameter (a subprocess-based named vendor needs one, unlike the HTTP-only vendors that existed before it), threaded through `project.BuildExecutorRegistry`. Routing is **explicit-pin-only**; capability-based negotiation is deferred (see RFC-0002 row below). **Actual-cost reporting shipped 2026-07-21** ([ADR-0011](../03-adrs/ADR-0011-cost-as-a-first-class-constraint.md)): `executor/openai.Executor` now decodes the Chat Completions response's `usage` object (previously received and discarded) into `domain.Outcome.ActualCostUSD`; `executor/claude.ClaudeExecutor` still reports none (its subprocess has no billing signal — a structural limit, not a gap). **"record/replay against real models" validated live 2026-07-22**: `replay/replay_live_test.go`'s `TestVerify_ReproducesARealClaudeCodeProducedAct` (skipped by default, `FOUNDRY_LIVE_TEST=1`-gated) runs a real `claude.ClaudeExecutor` end to end, records the Act, and confirms `replay.Verify` reproduces the recorded Judgment against the real, model-produced patch via `git apply` — closing the one genuine gap in this milestone's own name (every other replay test only replayed hand-built fixture patches). Confirmed passing live in this environment. |
| **M4** — Knowledge & evidence | Partial | Authored Knowledge write + naive lexical retrieval shipped. [ADR-0007](../03-adrs/ADR-0007-knowledge-and-semantic-store.md) (ratified 2026-07-21) closes the store's policy questions — note format, `.foundry/knowledge/` durability, format stability — but Derived Knowledge, semantic retrieval, and provenance scoring remain explicitly declined until each has a named trigger, so this milestone stays code-partial. |
| **M5** — Integration & visibility | Shipped, partially | VCS/PR publish shipped. **Copilot code review delegation shipped 2026-07-23**: `vcs.GitHubPRApplier` gained an opt-in `RequestCopilotReview` field (`.foundry/config.json`'s `request_copilot_review`) — after opening a pull request, it also runs `gh pr edit --add-reviewer @copilot` (GA since 2026-03-11). A failure to request the review (no paid Copilot plan, feature not enabled) is printed as a warning, never returned as an Apply error — the pull request, Foundry's own terminal action, already exists by that point. **Worktree isolation for concurrent Acts shipped 2026-07-20**: `workspace.Workspace` now realizes its throwaway branch inside a separate `git worktree`, never switching the developer's own checked-out branch — two Acts against the same repo no longer race (`workspace/workspace_test.go`'s `TestWorkspace_TwoWorkspaces_SameRepo_NoInterference`). **Change-review UI: a first real slice shipped 2026-07-21** — `foundry show` now colors its patch/verdict exactly like the live approval prompt, and renders a per-Step trace (`domain.Act.Steps`: each declared Step's own verdict and duration) that was already recorded but not previously surfaced, closing a real gap for multi-verify Pipelines like the built-in `review` Pipeline (`cli/cli.go`'s `formatAct`, `cli/render.go`'s `renderStepVerdictLabel`). **Observability: a first slice shipped 2026-07-21** — an opt-in (`FOUNDRY_LOG` env var) structured JSON event log to stderr via `engine.SlogReporter`, fanned out alongside the existing human `ProgressReporter` via a new `engine.MultiReporter`, wired at both composition roots (`cmd/foundry/commands/do.go`, `session/run_pipeline_command.go`) through one shared `cli.NewReporter` helper. Default behavior (env var unset) is byte-for-byte unchanged. Every milestone theme now has at least a first shipped slice. **Paging for long diffs/findings shipped 2026-07-21**: `foundry show` and the live approval prompt now pipe content longer than 40 lines through `$PAGER` (falling back to `less -R`, preserving color) when output is an interactive terminal — `cli/pager.go`'s `maybePage`, gated by the same explicit `page bool` pattern `renderDiff`/`renderVerdict` already established rather than re-deriving terminal-ness internally; a pager failure (not installed, broken `$PAGER`) always falls back to printing directly, never losing output. **Actual-cost reporting shipped 2026-07-21** ([ADR-0011](../03-adrs/ADR-0011-cost-as-a-first-class-constraint.md)): `foundry show` renders an "Actual cost" line (with honest "N of M generate Steps" coverage) whenever at least one generate Step's Executor reported a real, post-execution cost — omitted entirely otherwise, never a permanent placeholder; `engine.SlogReporter`'s new `act.execute.done` event carries the same signal into the `FOUNDRY_LOG` stream. **AI code review as a supplementary Verifier shipped 2026-07-23**, closing the maintainer's own follow-up question (answered "yes" first, then implemented on approval) about whether code review could be done by a model other than Copilot, with its findings feeding into fixes: new `verify.Compose` (`verify/compose.go`) is a general-purpose `engine.Verifier` combinator — any verdict other than "pass" fails the whole composition, findings concatenate — mirroring `gatherer.Compose`'s existing pattern exactly, so no new composition concept was invented. New `verify/aireview` package implements `engine.Verifier` against any OpenAI-Chat-Completions-compatible endpoint (reusing the same request/response shapes `executor/openai` already established, not a new HTTP client design), asking a model to review a proposed unified diff and respond with a parseable `VERDICT: pass|fail` plus findings; a response it cannot parse into a clear verdict is always treated as `fail`, never silently passed through, with the raw response preserved in the findings for debugging. Wired at both composition roots (`session.NewSession`, `cmd/foundry/commands/do.go`'s `wireEngine`) as `verify.Compose(gate, aireview.NewVerifier(...))` — never replacing the deterministic Gate, only supplementing it, per [trust.md](../02-architecture/trust.md)'s stated preference for deterministic checks first. `project.Config` gained three opt-in fields (`ai_review_model`, `ai_review_base_url`, `ai_review_api_key_env`); setting the model without a base URL is a clear, named configuration error at session/wireEngine construction time (no single default endpoint exists across vendors to fall back to) rather than a silently-inert verifier. No new Pipeline Step kind or ADR needed: a failed verify Step's findings already flow into the next generate attempt's considered Context via the existing repair mechanism (`engine/strategy.go`'s `repairContext`), so an AI reviewer's findings become fix context "for free." New tests: `verify/compose_test.go` (5 cases: no-verifiers passes, identical single verifier, all-pass yields pass, any failure fails the whole composition, an error stops composition), `verify/aireview/aireview_test.go` (8 cases covering pass/fail/malformed/transport-error/unauthorized/case-insensitive-verdict/empty-content), `session.TestNewSession_AIReviewModelRequiresBaseURL`, `cmd/foundry/commands.TestWireEngine_AIReviewModelRequiresBaseURL`, `project.TestLoadConfig_DecodesAIReviewFields`. **Not validated live**: no real OpenAI/Gemini/Ollama/Groq/DeepSeek endpoint or key available in this environment — fixture-tested against the documented Chat Completions API shape only. `go build ./...`, `go vet ./...`, `go test -race ./...` all green; `gofmt -l .` clean. |
| **M6** — Extensibility | Not started | No third-party extension surface exists. **[ADR-0008](../03-adrs/ADR-0008-extension-isolation-and-contract-versioning.md) (ratified 2026-07-21) resolves the milestone's one open policy question** (isolation mechanism, contract versioning) as a deliberate non-decision — no mechanism chosen, deferred to a real third-party extension request — rather than leaving it ambiguous; it also corrects [ADR-0001](../03-adrs/ADR-0001-language-and-toolchain.md)'s stale gRPC/protobuf pre-commitment. The milestone itself remains code-not-started; this ADR closes a documentation gap, not the milestone. |
| **M7** — Stability | Not started | No contract freeze; several open decisions below still gate it. |
| **Parallel track** — Interactive terminal UX | Shipped 2026-07-22, v4 | [ADR-0012](../03-adrs/ADR-0012-interactive-terminal-ux-and-first-dependency.md) (Accepted). **v1** (`session/repl.go`'s read loop, real terminal only via `session.Interactive`, same gate as `cli.colorEnabled`): Up/Down in-session history recall. **v2, per the maintainer's own reference (a Claude-Code-style startup panel and a full, navigable "/" command menu)**: replaced the single-suggestion inline ghost-text with a full, arrow-navigable dropdown (`cli/interactive_prompt.go`'s `promptModel`) listing every registered slash command with its description, opening the moment the line starts with `/`, filtered to matching prefixes as the user keeps typing, closing once the line is no longer a bare command name (a space, or an exact match with nothing left to complete — a real bug a live pty run caught: without the exact-match rule, finishing a command by typing it in full still needed a redundant second Enter just to accept what was already fully typed). New `cli/interactive_renderer.go` `Banner(root, initialized)`: a `lipgloss`-bordered panel showing the real project root, `cli.BuildVersion()` (the binary's actual embedded VCS revision via `runtime/debug.ReadBuildInfo`, never a fabricated semantic version — Foundry has no release process yet), and whether `/init` has already scaffolded this project (`session.Session.Initialized`, checking `project.PipelinesDir` on disk) — a non-terminal `Out` (every existing test) gets the same three facts as plain lines, no border, no ANSI. Confirmed live in a real pty (Python's `pty` module, answering the startup OSC-11/DSR terminal queries): the banner renders correctly bordered and colored; typing `/` opens the full 8-item menu with the first entry highlighted; Down moves the highlight through the list; Enter accepts a highlighted-but-partial selection (appends a trailing space, keeps the menu closed, does not dispatch) while a single Enter on an exactly-typed command (e.g. `/exit`) dispatches/exits immediately; `/exit` ends the process cleanly (exit status 0). **v3**: `cli.PromptHistory` gained `LoadPromptHistory(path)`, reading a project's `.foundry/history` (new `session.HistoryFile` constant) at REPL construction and appending every submitted line back to it immediately (best-effort — a write failure never breaks the session, since this is convenience state, not Evidence or Knowledge; documented in [getting-started.md](../04-guides/getting-started.md) as something to gitignore, unlike `.foundry/acts`/`.foundry/pipelines`/`.foundry/knowledge`). `NewPromptHistory` (in-memory only, no file) is unchanged for any caller that doesn't want persistence. Confirmed live across two separate process runs against the same repo: session 1 submitted `/help`, `/bogus`, `/exit`, writing exactly those three lines to `.foundry/history`; a fresh session 2 process recalled them via Up (most-recent-first: `/exit`, then `/bogus`). **v4**: `cli/progress.go`'s live narration (`Gathering`/`Executing`/`Verifying`/`Repairing`/`RepairSkipped`/`BudgetExceeded`) restyled via `lipgloss` (bold cyan for in-progress action lines, bold yellow for repair, bold red for errors, dim gray for the findings-truncation notice), replacing the raw `ansiCyan`/`ansiYellow`/`ansiRed` string-concatenation approach — closing the last visual inconsistency between the interactive session's newly-chrome'd banner/menu and its live progress output. Deliberately scoped to this one file: `render.go`'s `renderVerdict`/`renderDiff`/`renderStepVerdictLabel`/`formatReplayResult` (shared by `foundry show`/`replay`, with existing byte-exact golden tests) are left on the original ANSI-constant mechanism — restyling those has no functional benefit and real regression risk for a purely cosmetic change. Not part of the M0–M7 depth sequence — orthogonal, neither blocks nor is blocked by M6/M7. |
| **Parallel track** — Ticket-driven Acts (`/issue`) | Shipped 2026-07-23, all four providers |  New `/issue <id>` slash command: fetches an external ticket's title/description and runs it as an Act's Intent, instead of a human typing one — per the maintainer's own description of a Claude-Code-style `/issue <ticket>` workflow, diversified across models. New `ticket` package (`Fetcher` interface, vendor-agnostic `Issue` struct) and `ticket/github` (shells out to `gh issue view <id> --json number,title,body,url`, reusing the exact same already-authenticated `gh` session `vcs.GitHubPRApplier`'s own PR-opening already requires — Foundry reads no separate credential). `project.Config` gained `TicketProvider` (`ticket_provider`, "github" today); `project.TicketFetcherConstructor` mirrors `ExecutorConstructor`'s own vendor-dispatch seam; `session.Session` gained `SetTicketFetcher` (a plain setter, not threaded through `NewSession`'s constructor, to avoid rippling its signature across ten existing call sites). `session.IssueCommand` fetches, formats the Issue as Intent text (title + description + a `(Source: <url>)` trailer so `foundry show` can trace back to the ticket), and delegates the entire generate/verify/approve/apply/record lifecycle to the *existing*, unmodified `RunPipelineCommand` — the only new logic is where the Intent's text comes from. Deliberately **not** a new Step kind: RFC-0002 §4.2's closed five stay exactly as they are; fetching happens before a Pipeline starts, the same way a slash command's own typed argument text already does — this is a new instance of the already-named "Context Source" extension point ([extensibility.md](../02-architecture/extensibility.md)), needing no new ADR, the same way another Executor vendor doesn't. A new `.foundry/pipelines/issue.json` (this repo's own dogfooded copy — richer than what `/init` scaffolds for other projects, mirroring `feature.json`'s own precedent) targets `remote-pr` on its apply Step, matching the maintainer's own described end state ("preguntar si se quiere mandar el PR directamente"); `/init`'s own scaffolded starter for other projects gets the same minimal `generate → verify` shape every other starter gets. **`ticket/jira` added the same day**, per the maintainer's own stated priority order (GitHub, then Jira, then GitLab/Asana): a pure HTTP call against Jira Cloud's REST API v3 (`GET /rest/api/3/issue/{id}?fields=summary,description`), authenticating with HTTP Basic Auth (`base64(email:api_token)`, Atlassian's own documented scheme) — Jira has no equivalent already-authenticated CLI session to piggyback on the way GitHub's `gh` does, so `project.Config` gained three more fields (`JiraBaseURL`/`jira_base_url`, `JiraEmail`/`jira_email`, `JiraAPITokenEnv`/`jira_api_token_env`, the last never storing the token itself, mirroring `ExecutorConfig.APIKeyEnv`'s pattern). A real, non-obvious wrinkle this provider needed that GitHub's didn't: Jira Cloud's `description` field is **Atlassian Document Format (ADF)** — a nested JSON rich-text tree (`{type, content, text}` nodes), not a plain string — so `ticket/jira` includes a small recursive `adfToPlainText` walker extracting readable plain text (concatenating `text` nodes, inserting a newline after block-level nodes like `paragraph`/`heading`/`listItem`/`codeBlock`), confirmed against Atlassian's own documented minimal ADF example before writing it, not guessed. `cmd/foundry/main.go`'s `newTicketFetcher` gained a `"jira"` case. New tests (`ticket/jira/jira_test.go`): success with a real-shaped ADF description parsed into multi-paragraph plain text, a null description (an issue with none at all) yielding an empty string rather than an error, empty id, no base URL configured, transport error, 401 (with Jira's own `errorMessages` array surfaced), 404, and a non-JSON error body; `cmd/foundry/ticket_fetcher_test.go` and `project/config_test.go` extended for the new vendor/fields. **Not validated live**: no Jira Cloud site or API token available in this environment — fixture-tested against Atlassian's own documented request/response shapes (auth header format and a minimal ADF example both confirmed via their official docs before implementing, not assumed from memory) only, unlike the GitHub path above, which was confirmed against a real issue. **`ticket/gitlab` and `ticket/asana` added the same day**, completing the maintainer's own stated priority order. `ticket/gitlab` mirrors `ticket/github` exactly — shells out to `glab issue view <id> --output json` (glab's own documented flag, confirmed via its docs before implementing) instead of a raw token, needing no new `project.Config` fields at all (`ticket_provider: "gitlab"` alone suffices, the same zero-extra-config shape as GitHub). `ticket/asana` mirrors `ticket/jira`'s pure-HTTP shape — `GET /tasks/{gid}?opt_fields=gid,name,notes,permalink_url`, Bearer Personal Access Token — but is simpler than Jira in two ways confirmed against Asana's own docs: no rich-text document format to parse (`notes` is already plain text, unlike Jira's ADF `description`), and no separate base URL to configure (`app.asana.com` is one fixed global endpoint regardless of workspace) — `project.Config` gained only `AsanaAPITokenEnv`/`asana_api_token_env`. `cmd/foundry/main.go`'s `newTicketFetcher` gained `"gitlab"` and `"asana"` cases. New tests: `ticket/gitlab/gitlab_test.go` and `ticket/asana/asana_test.go`, each mirroring their respective sibling provider's own test structure (success with request captured, empty/malformed id, vendor failure propagation, unparseable/error response bodies, 401/404 for Asana's HTTP path); `cmd/foundry/ticket_fetcher_test.go` and `project/config_test.go` extended for both. **Not validated live**: no `glab` binary installed (deliberately not installed without asking first, since that would modify the host system beyond this repository) and no Asana account/token available in this environment — both fixture-tested against their vendors' own documented shapes only. All four ticket providers the maintainer asked for are now shipped; GitHub alone has been confirmed against a real ticket, live. `go build ./...`, `go vet ./...`, `go test -race ./...` all green; `gofmt -l .` clean.

  **Validated live in multiple ways**: (1) `ticket/github.Fetcher` run directly (not through a unit test) against a real, current GitHub issue (`golang/go` issue #80554, a real `x/tools` bug report) via the real, authenticated `gh` CLI already present in this environment — correctly fetched and decoded the real ID, title, URL, and description. (2) A full interactive-session pty run (`/init` then `/issue 80554` against a throwaway repo configured with `"ticket_provider": "github"`) confirmed the fetch succeeded and the built Intent correctly reached the Engine and the real Claude Code executor (`→ Gathering repository context...` then `→ Asking Claude Code for a patch...`, both live, real output) — proving the fetch → Intent → Engine dispatch chain works end to end. **What this pass did not confirm**: the run through to a final verdict and approval prompt, twice, because the specific real issue chosen (a genuine, complex `golang/go` tooling bug) describes a fix against source code nowhere present in the trivial throwaway test repo — Claude Code spent longer reconciling that mismatch than this test's patience allowed, a test-scenario mismatch on this session's part, not a discovered Foundry defect; the downstream generate → verify → approve → apply → record flow itself is the same `RunPipelineCommand`/Engine code path already validated live, repeatedly and successfully, earlier in this session. New tests: `ticket/github/github_test.go` (success with args/JSON captured, empty/non-numeric id, gh failure propagation, unparseable output); `project/config_test.go`'s `TestLoadConfig_DecodesTicketProvider`; `cmd/foundry/ticket_fetcher_test.go`'s `TestNewTicketFetcher_GithubVendorConstructsGithubFetcher`/`_UnsupportedProviderFails`; `session/issue_command_test.go` (empty args, no fetcher configured, fetch failure propagation, and a full happy-path proving the fetched Issue's title/URL land correctly in the recorded Act's Intent). `project.ProjectLoader.Scaffold`'s starter set and `session.DefaultCommandRegistry`'s `/help` listing both extended to include `issue`/`/issue`; `session.InitCommand`'s confirmation message updated to name it alongside `/feature`/`/bug`/`/release`. `go build ./...`, `go vet ./...`, `go test -race ./...` all green; `gofmt -l .` clean. |

Getting-started instructions for what's usable today live in [../04-guides/getting-started.md](../04-guides/getting-started.md).

## 2. RFCs — decision status vs. code status

All five RFCs remain **Draft — Proposed**: a governance process exists ([ADR-0000](../03-adrs/ADR-0000-governance-and-ratification-process.md)), but none has been individually ratified through it yet. Their *implementation* status varies widely and is tracked here so "unratified" is never misread as "unbuilt."

| RFC | Ratified? | Implementation | Pending |
|---|---|---|---|
| [RFC-0001](../01-rfcs/RFC-0001-vision-and-product-philosophy.md) — Vision & product philosophy | No | N/A — a values/philosophy document, not a code deliverable; already reflected throughout the architecture docs and the system's actual behavior (trust gate, human approval, deterministic-first). | Formal ratification only. |
| [RFC-0002](../01-rfcs/RFC-0002-pipeline-execution-runtime.md) — Pipeline execution runtime | No | **~85%.** Phases 1–6 and 8 shipped: Step trace on `Act`, `Strategy`/`PipelineStrategy`, Approve/Record as declared Step kinds, `FixedStrategy` fully retired, Router (explicit-pin), interactive shell. **Both of Phase 0's prerequisite ADRs now accepted and implemented:** [ADR-0005](../03-adrs/ADR-0005-executor-contract-and-capability-model.md) (Executor contract) and [ADR-0004](../03-adrs/ADR-0004-reusable-act-template-format-and-evolution-policy.md) (Reusable-Act template format, ratified and its Decision 4 shipped 2026-07-20 — §3 below). **§7's routing-policy gap is now closed**: [ADR-0006](../03-adrs/ADR-0006-routing-and-policy.md) (drafted and ratified 2026-07-20 — §3 below) ratifies today's explicit-pin Router and reaffirms that negotiation/failover stay deferred. | **Phase 7** — capability-based negotiation/failover (cost/latency/quality weighting) — deliberately deferred by [ADR-0006](../03-adrs/ADR-0006-routing-and-policy.md) itself, no real multi-Executor-per-Capability case yet. |
| [RFC-0003](../01-rfcs/RFC-0003-interactive-assistant-and-multi-executor-pipelines.md) — Interactive assistant & multi-executor pipelines | No | **~90%.** Interactive session & slash commands (§3.1), project-local Pipeline authoring via `/init` (§3.2), VCS/PR automation (§4.1, [ADR-0010](../03-adrs/ADR-0010-vcs-pr-integration-and-apply-targets.md)) all shipped. **§6's "CLI & output contract" gap is now closed**: [ADR-0009](../03-adrs/ADR-0009-cli-and-output-contract.md) (drafted and ratified 2026-07-20 — §3 below) formally ratifies the interactive-primary product-shape direction the maintainer confirmed informally on 2026-07-18, and all its Decisions, including 6–8, are implemented the same day: `/help` is a real command (`session.HelpCommand`), `engine.PipelineProvider`/`BuiltinProvider`/`project.FilesystemPipelineProvider` are renamed to `PipelineSource`/`BuiltinPipelineSource`/`FilesystemPipelineSource`, and Session/Slash Command are documented in [terminology.md](../05-reference/terminology.md). | Same gap as RFC-0002: real capability-based routing validated against a multi-role lifecycle (§3.3) is not built — today's Router is explicit-pin, two vendors. |
| [RFC-0004](../01-rfcs/RFC-0004-multi-executor-router-and-publish-policy.md) — Multi-executor router & publish policy | No | **100% of its own 6 Pieces shipped** (Capability/Router/ExecutorRegistry, [ADR-0005](../03-adrs/ADR-0005-executor-contract-and-capability-model.md) accepted, second Executor OpenAI, Knowledge-lite capture, per-Step budget accounting, VCS/PR via [ADR-0010](../03-adrs/ADR-0010-vcs-pr-integration-and-apply-targets.md) accepted). §4's "Routing & policy" backlog ADR is now drafted and ratified as [ADR-0006](../03-adrs/ADR-0006-routing-and-policy.md). | Nothing code-side; only the RFC document's own ratification. |
| [RFC-0005](../01-rfcs/RFC-0005-authored-knowledge-retrieval.md) — Authored Knowledge retrieval | No | **100% of its own 4 pieces shipped** (formalized `.foundry/knowledge/` as the Authored store, `gatherer.Compose`, naive lexical Knowledge Gatherer, wired into both composition roots). | Nothing code-side for what this RFC proposed. Explicitly out of its scope, and not yet proposed by any RFC: Derived Knowledge indexing, semantic retrieval, Authored-knowledge format stability. |

## 3. ADRs

Accepted vs. backlog is tracked canonically in [../03-adrs/README.md](../03-adrs/README.md) — this section only flags what's implementation-relevant beyond that table:

- **Accepted (13):** [ADR-0000](../03-adrs/ADR-0000-governance-and-ratification-process.md) (governance), [ADR-0001](../03-adrs/ADR-0001-language-and-toolchain.md) (Go — both pending amendments now resolved, see ADR-0002 and ADR-0008 below), [ADR-0002](../03-adrs/ADR-0002-persistence-content-addressing-and-on-disk-layout.md) (Persistence, content-addressing & on-disk layout — ratified 2026-07-20, no code changes needed), [ADR-0003](../03-adrs/ADR-0003-replay-and-determinism-contract.md) (Replay & determinism contract — ratified 2026-07-20, no code changes needed), [ADR-0004](../03-adrs/ADR-0004-reusable-act-template-format-and-evolution-policy.md) (Reusable-Act template format — ratified and implemented 2026-07-20), [ADR-0005](../03-adrs/ADR-0005-executor-contract-and-capability-model.md) (Executor contract — implemented), [ADR-0010](../03-adrs/ADR-0010-vcs-pr-integration-and-apply-targets.md) (VCS/PR — implemented), [ADR-0009](../03-adrs/ADR-0009-cli-and-output-contract.md) (CLI & output contract — ratified and implemented 2026-07-20), [ADR-0006](../03-adrs/ADR-0006-routing-and-policy.md) (Routing & policy — ratified 2026-07-20, no code changes needed), [ADR-0007](../03-adrs/ADR-0007-knowledge-and-semantic-store.md) (Knowledge & semantic store — ratified 2026-07-21, no code changes needed), [ADR-0011](../03-adrs/ADR-0011-cost-as-a-first-class-constraint.md) (Cost as a first-class constraint — ratified 2026-07-21, follow-up implementation PR shipped same day), [ADR-0008](../03-adrs/ADR-0008-extension-isolation-and-contract-versioning.md) (Extension isolation & contract versioning — ratified 2026-07-21, no code changes needed), [ADR-0012](../03-adrs/ADR-0012-interactive-terminal-ux-and-first-dependency.md) (Interactive terminal UX & Foundry's first third-party dependency — drafted and ratified 2026-07-22 the same day, once the maintainer confirmed `bubbletea`/`bubbles`/`lipgloss` directly; implemented same day). The ADR backlog is empty again; new rows are added only as new architectural decisions are genuinely identified as needed. ADR-0008 decides no isolation mechanism and no versioning policy (both genuinely undecided — no third-party extension exists to design against); its real work corrects [ADR-0001](../03-adrs/ADR-0001-language-and-toolchain.md) clause 4 (and downstream Consequences/"Harder"/Future-ADR-Dependencies text), which had asserted gRPC/protobuf as a **binding** decision even after [extensibility.md](../02-architecture/extensibility.md) and [OQ-005](../06-open-questions/OQ-005-extension-isolation.md) had already withdrawn that assertion as premature — the same class of live contradiction [ADR-0002](../03-adrs/ADR-0002-persistence-content-addressing-and-on-disk-layout.md) corrected for ADR-0001's SQLite clause. R2 (language-agnostic extension boundary) remains fully binding; the concrete mechanism is deferred to a real third-party extension request. ADR-0011 closes [ADR-0005](../03-adrs/ADR-0005-executor-contract-and-capability-model.md)'s Open Question 1 with a firm "no" (`CostEstimator` stays optional — Claude Code's subprocess structurally has no billing signal); proposes optional, additive `ActualCostUSD` on `Outcome`/`StepRecord`/`Act` as reported Evidence only, never a second Budget gate; declines to make Budget's hardcoded ceilings configurable or to build cost reconciliation until each has a named trigger. ADR-0009 ratifies the interactive-primary product shape, a pre-1.0 "change freely, but never silently" policy for the flag CLI and slash commands, the existing exit-code convention, and declines a JSON output mode for now; fixes the phantom `/help` banner reference and renames `PipelineProvider`→`PipelineSource` (RFC-0003 §5's flagged terminology tension) — see the RFC-0003 row above and the ADR's own Review Checklist. ADR-0006 ratifies the already-shipped explicit-pin `Router`/`ExecutorRegistry` and the Router-reserved Step fields' behavior; reaffirms [ADR-0005](../03-adrs/ADR-0005-executor-contract-and-capability-model.md) Decision 4 ("capability declared, not verified"); explicitly declines to build capability-based negotiation or failover before their own named trigger fires; fixes README.md's live "Provider independence" defect — see the RFC-0002/RFC-0004 rows above and the ADR's own Review Checklist. ADR-0002 ratifies `record.FileStore`/`record.CheckpointStore` exactly as already shipped, closes [OQ-008](../06-open-questions/OQ-008-in-progress-act-persistence.md), gives [I8](../05-reference/invariants.md) a concrete consequence (commit `.foundry/acts/` to a project's own repository), and **corrects a real contradiction**: [ADR-0001](../03-adrs/ADR-0001-language-and-toolchain.md) had pre-committed it to an embedded pure-Go SQLite storage driver — a design that was never built and directly conflicted with [principles.md](principles.md)'s higher-precedence Filesystem-first mandate; ADR-0001's two affected clauses are now corrected in place. ADR-0003 ratifies `replay.Verify` exactly as already shipped (same-version-only; real Validators/Gate re-executed against the recorded Patch, never the Executor; a verdict divergence surfaced as honest data, never hidden); closes [OQ-003](../06-open-questions/OQ-003-replay-across-versions.md) and [OQ-004](../06-open-questions/OQ-004-validator-determinism.md) together, **explicitly superseding OQ-004's own recommendation** (record-and-replay validator findings, never re-run the tool) with the stronger design actually built. ADR-0007 ratifies `workspace.KnowledgeNoteApplier`/`workspace.ProjectDocApplier`/`knowledge.Gatherer` exactly as already shipped (unstructured Markdown prose, no front-matter); closes the Authored-Knowledge format-stability question by observing unparsed text has nothing to destabilize; extends ADR-0002's `.foundry/acts/` commit-to-git convention to `.foundry/knowledge/`; and **explicitly declines** Derived-Knowledge indexing, semantic retrieval, provenance scoring, and note curation until each has a named, concrete trigger, rather than leaving them ambiguous.
- **Drafted, pending ratification (0):** none currently.
- **Backlog, implementation-relevant:** capability-based negotiation and failover (RFC-0002 §7 layers 2–3) remain unbuilt regardless of [ADR-0006](../03-adrs/ADR-0006-routing-and-policy.md)'s ratification status — that ADR explicitly defers both until their own named trigger (≥2 real Executors competing for one Capability in production) fires, not until the ADR itself is accepted.

## 4. Architecture documents (`docs/02-architecture/`)

All PROVISIONAL (nothing in the repository is CANONICAL yet — [ADR-0000](../03-adrs/ADR-0000-governance-and-ratification-process.md) made the ceiling reachable, but each document still needs its own ratification). Maturity nuance for each is tracked in [../05-reference/concepts.md](../05-reference/concepts.md)'s maturity index — this table adds *implementation* coverage, which that index doesn't track:

| Document | Answers | Implementation coverage |
|---|---|---|
| [domain.md](../02-architecture/domain.md) | What are the domain concepts? | High — Act, Intent, Evidence, Outcome, Judgment, Authority, Record all exist as real types (`domain/act.go`) and are exercised end-to-end. |
| [execution.md](../02-architecture/execution.md) | How does the system produce outcomes? | High — Pipeline-as-Strategy, Steps, repair are all real (`engine/strategy.go`, `engine/document.go`). |
| [trust.md](../02-architecture/trust.md) | How is trust established? | High for the mechanics (Verify/Gate/Judgment/Approve all run today); the *verification guarantee's* exact strength remains [OQ-004](../06-open-questions/OQ-004-validator-determinism.md), unresolved by design. |
| [knowledge.md](../02-architecture/knowledge.md) | What is durable knowledge? | Partial — Authored Knowledge (write + naive retrieval) is real; Derived Knowledge is pure concept, no code. |
| [system-context.md](../02-architecture/system-context.md) | What are the system boundaries? | Partial — today's actual (local, single-user) boundary is accurately described; the "remote is additive" and concurrency claims are unproven, no code exercises them. |
| [extensibility.md](../02-architecture/extensibility.md) | What may be extended? | Low — requirements only. No third-party extension surface exists (M6 not started); the isolation mechanism is deliberately undecided, ratified as such by [ADR-0008](../03-adrs/ADR-0008-extension-isolation-and-contract-versioning.md) (closes [OQ-005](../06-open-questions/OQ-005-extension-isolation.md)). |

## 5. Guides (`docs/04-guides/`)

| Guide | Status | Use it for |
|---|---|---|
| [getting-started.md](../04-guides/getting-started.md) | Active | Installing and running Foundry today — the actual first stop for a new user. |
| [pipelines.md](../04-guides/pipelines.md) | Active | Authoring/reading a Pipeline document. |
| [development.md](../04-guides/development.md) | Active | Durable build/test/CI approach (not milestone-specific). |
| [m0-plan.md](../04-guides/m0-plan.md) | Historical (kept as design rationale) | *Why* M0 was scoped the way it was — not a current plan. |
| [multi-executor-router-implementation-plan.md](../04-guides/multi-executor-router-implementation-plan.md) | Historical (all 6 Pieces shipped) | Reference for *how* RFC-0004 was actually sequenced. |
| [interactive-session-implementation-plan.md](../04-guides/interactive-session-implementation-plan.md) | Check before reuse | Sequencing plan for RFC-0003's interactive session — verify against §2 above before treating any step as still-pending. |
| [contributing.md](../04-guides/contributing.md), [documentation.md](../04-guides/documentation.md), [release.md](../04-guides/release.md) | Active | Process guides, not milestone-specific — not audited for staleness here. |

## 6. Reference & open questions — pointers only

- Concept-by-concept maturity: [../05-reference/concepts.md](../05-reference/concepts.md) (do not duplicate here — that file is the canonical maturity index).
- Canonical vocabulary: [../05-reference/terminology.md](../05-reference/terminology.md).
- Invariants: [../05-reference/invariants.md](../05-reference/invariants.md).
- Open architectural questions (non-canonical, active deliberation): [../06-open-questions/README.md](../06-open-questions/README.md).

## 7. Real-world validation log

Distinct from the audits above (which read code and docs): this tracks actual runs of the built binary against a real repository with a live Executor — the strongest evidence available that the walking skeleton still works end-to-end, not just that its unit tests pass in isolation.

- **2026-07-18** — Ran a full interactive session (`/init` → `/bug "<intent>"` → real Claude Code Executor → `go build`/`go test` verification → human approval → apply → record) against a throwaway Go-module sample repository. Confirmed working end-to-end: real patch generation, verification, approval, Act recording, and both `foundry log`/`foundry show` history inspection all matched their documented behavior. Surfaced one real bug in the process — the `go-build` validator (`verify/detect.go`) left a compiled binary artifact in the repository on every Act against a `package main` module — fixed same day (`fix(verify): Stop go-build validator from leaking a binary into the repo`).

## Changelog

- **2026-07-18** — Initial audit. Archived the stale M0 checklist docs; added this file; registered the confirmed interactive-primary product-shape direction against ADR-0009 in [../03-adrs/README.md](../03-adrs/README.md).
- **2026-07-18** — First real dogfooding run (§7): validated the walking skeleton end-to-end with a live Claude Code Executor; found and fixed a `go-build` validator bug that leaked a compiled binary into the repository on every Act.
- **2026-07-20** — Drafted and ratified [ADR-0004](../03-adrs/ADR-0004-reusable-act-template-format-and-evolution-policy.md) (Reusable-Act template format & evolution policy) under [ADR-0000](../03-adrs/ADR-0000-governance-and-ratification-process.md), satisfying RFC-0002 Phase 0's second prerequisite ADR.
- **2026-07-20** — Implemented ADR-0004 Decision 4: `engine.DecodePipelineDocument` now rejects unknown fields with a descriptive error naming the field, the enclosing Step (or index), and pointing at [pipelines.md](../04-guides/pipelines.md); documented in that guide's new "Field evolution and unknown fields" section.
- **2026-07-20** — Second full audit. Re-verified every roadmap milestone, RFC, and ADR row against the actual codebase (`engine/router.go`, `engine/executor_registry.go`, `knowledge/`, `vcs/`, absence of any worktree-isolation or extension-surface code) and against [roadmap.md](roadmap.md) — no drift found beyond the ADR-0004 rows already updated above.
- **2026-07-20** — Drafted [ADR-0009](../03-adrs/ADR-0009-cli-and-output-contract.md) (CLI & output contract), closing RFC-0003 §6's named gap. Status: Proposed, not yet ratified.
- **2026-07-20** — Implemented ADR-0009 Decisions 6–8: `session.HelpCommand` backs `/help` (listing every registered slash command via a new `CommandRegistry.List`); `engine.PipelineProvider`/`BuiltinProvider` and `project.FilesystemPipelineProvider` are renamed to `PipelineSource`/`BuiltinPipelineSource`/`FilesystemPipelineSource` throughout the codebase and [pipelines.md](../04-guides/pipelines.md); **Session** and **Slash Command** added to [terminology.md](../05-reference/terminology.md) and [concepts.md](../05-reference/concepts.md). Decisions 2 and 4 (pre-1.0 change policy, exit-code convention) documented in [release.md](../04-guides/release.md)'s new "CLI & output contract" section.
- **2026-07-20** — Ratified [ADR-0009](../03-adrs/ADR-0009-cli-and-output-contract.md) (CLI & output contract) under [ADR-0000](../03-adrs/ADR-0000-governance-and-ratification-process.md), the same day it was drafted and implemented. Status row, [README.md](../03-adrs/README.md)'s backlog table, and this file all updated in the same ratifying pass; `release.md`'s "intended, not ratified" disclaimer updated to reflect the CLI & output contract surface is no longer blocked.
- **2026-07-20** — Drafted and ratified [ADR-0006](../03-adrs/ADR-0006-routing-and-policy.md) (Routing & policy) under [ADR-0000](../03-adrs/ADR-0000-governance-and-ratification-process.md), the same day, closing RFC-0002 §7 / RFC-0004 §2.1–§2.3's named routing-policy gap. Ratifies the already-shipped explicit-pin `Router`; formally re-examines and reaffirms [ADR-0005](../03-adrs/ADR-0005-executor-contract-and-capability-model.md) Decision 4 per that ADR's own named requirement to do so; closes [ADR-0004](../03-adrs/ADR-0004-reusable-act-template-format-and-evolution-policy.md) Decision 7's deferral on the Router-reserved Step fields' behavior; declines to build capability-based negotiation or failover before their own named trigger fires. Also fixes a live use of the retired term "Provider" in [README.md](../03-adrs/README.md)'s backlog table ("Provider independence" → "Vendor independence"). No code changes required — the ADR ratifies already-shipped, already-tested behavior.
- **2026-07-20** — Shipped M5's "Worktree isolation for concurrent Acts": `workspace.Workspace` (`workspace/workspace.go`) now creates its throwaway branch inside a separate `git worktree` — mirroring `StagedVerifier`'s already-proven pattern (`workspace/staged.go`) — instead of `git checkout -b` directly in the developer's repository. The developer's own checked-out branch is never switched for any part of an Act's lifetime; only `Land`'s single, final `git apply` ever touches it. Fixes two latent bugs as a side effect: (1) `Clean` no longer runs `git reset --hard`/`git clean -fd` against the developer's real directory, so any of the developer's own pre-existing uncommitted changes now survive a discarded Act; (2) `Land` now checks that the repository's branch hasn't changed since the Act started, refusing (rather than silently misapplying) if the developer switched branches mid-Act — on any failure, the worktree/branch/patch file are preserved for manual recovery, never destroyed. `workspace/staged.go` also got a one-line fix for the same root cause (its worktree directory's leaf name could collide with a concurrent verification's under git's own internal `.git/worktrees/<name>` naming). No public API changes — `ApplyAct`, `GitApplier`, and `vcs.GitHubPRApplier` are all untouched. Verified with `go test -race ./...` (new test: `TestWorkspace_TwoWorkspaces_SameRepo_NoInterference`) and a manual smoke run. A gap was surfaced during this work (see next entry).
- **2026-07-20** — Fixed the gap surfaced above: `cli.go`'s `Do`/`finalize` never recorded an approved Act if `workspace.ApplyAct` failed after approval (a tension with I8, immutable history) — investigation confirmed this only affects the CLI's own direct-apply fallback (a Pipeline declaring no apply Step, e.g. the built-in `default`/`review` Pipelines): a Pipeline that declares its own `apply`/`record` Steps already recovers correctly via `foundry resume` (its checkpoint, saved after the preceding `approve` Step, lets `engine.Resume` retry the failed `apply` Step exactly), but `engine.Resume` refuses outright once a Pipeline has no more declared Steps left to run, which is always true here by the time this fallback path executes. Fix: on an apply failure, `finalize` now sets a new `engine.VerdictApplyFailed` verdict, appends the error as a `CheckedFindings` entry, and records the Act — so the approval decision and patch survive even though the apply itself did not land — before returning the error. New test: `cli.TestCLI_Do_ApprovedButApplyFailsIsStillRecorded`.
- **2026-07-20** — Drafted [ADR-0002](../03-adrs/ADR-0002-persistence-content-addressing-and-on-disk-layout.md) (Persistence, content-addressing & on-disk layout), the first ADR in the backlog's dependency order. Ratifies `record.FileStore`/`record.CheckpointStore` exactly as shipped, closes [OQ-008](../06-open-questions/OQ-008-in-progress-act-persistence.md), and gives I8 a concrete practical consequence. Surfaced and corrects a real contradiction: [ADR-0001](../03-adrs/ADR-0001-language-and-toolchain.md)'s pre-commitment to an embedded pure-Go SQLite storage driver was never built and conflicts with [principles.md](principles.md)'s higher-precedence Filesystem-first mandate. Status: Proposed, not yet ratified.
- **2026-07-20** — Ratified [ADR-0002](../03-adrs/ADR-0002-persistence-content-addressing-and-on-disk-layout.md) under [ADR-0000](../03-adrs/ADR-0000-governance-and-ratification-process.md), the same day it was drafted. [ADR-0001](../03-adrs/ADR-0001-language-and-toolchain.md)'s two stale persistence clauses (its Decision section and its Consequences section) are corrected in place, pointing at ADR-0002 rather than describing an embedded SQLite driver that was never built; [OQ-008](../06-open-questions/OQ-008-in-progress-act-persistence.md) marked RESOLVED (and its own index row in [open-questions/README.md](../06-open-questions/README.md)); [roadmap.md](roadmap.md) open decision 6 marked RESOLVED; [invariants.md](../05-reference/invariants.md)'s closing note no longer lists I8 as pending; [release.md](../04-guides/release.md) gets a new "Record format & persistence" section and an updated disclaimer; [getting-started.md](../04-guides/getting-started.md) gains a commit-`.foundry/acts/`-to-git recommendation; [concepts.md](../05-reference/concepts.md) and [README.md](../03-adrs/README.md)'s backlog table updated to match.
- **2026-07-20** — Corrected a leftover drift from ADR-0002's ratification: [trust.md](../02-architecture/trust.md)'s own "Unresolved: the durability classification of the Record" callout was not updated in that pass — fixed now to cite ADR-0002.
- **2026-07-20** — Drafted [ADR-0003](../03-adrs/ADR-0003-replay-and-determinism-contract.md) (Replay & determinism contract), the second ADR in the backlog's dependency order. Ratifies `replay.Verify` exactly as shipped and closes OQ-003/OQ-004 together, explicitly superseding OQ-004's own never-built "record-and-replay validator findings" recommendation with the stronger, actually-shipped design (real Validators/Gate re-executed, divergence surfaced honestly). Status: Proposed, not yet ratified.
- **2026-07-20** — Ratified [ADR-0003](../03-adrs/ADR-0003-replay-and-determinism-contract.md) under [ADR-0000](../03-adrs/ADR-0000-governance-and-ratification-process.md), the same day it was drafted. [execution.md](../02-architecture/execution.md)'s "Determinism and replay" section corrected (cites this ADR and ADR-0002 by name, drops the stale "Unresolved" callout); [trust.md](../02-architecture/trust.md)'s remaining "Unresolved: validator determinism" callout resolved; [OQ-003](../06-open-questions/OQ-003-replay-across-versions.md) and [OQ-004](../06-open-questions/OQ-004-validator-determinism.md) marked RESOLVED (both their own files and [open-questions/README.md](../06-open-questions/README.md)'s index); [roadmap.md](roadmap.md) open decisions 4–5 marked RESOLVED; [invariants.md](../05-reference/invariants.md)'s closing note no longer lists I3/I6 as pending; [concepts.md](../05-reference/concepts.md) and [README.md](../03-adrs/README.md)'s backlog table updated to match. Only "Knowledge & semantic store," "Extension isolation & contract versioning," and "Cost as a first-class constraint" remain in the ADR backlog.
- **2026-07-21** — Drafted [ADR-0007](../03-adrs/ADR-0007-knowledge-and-semantic-store.md) (Knowledge & semantic store), the next ADR in the backlog's dependency order. Ratifies `workspace.KnowledgeNoteApplier`/`workspace.ProjectDocApplier`/`knowledge.Gatherer` exactly as shipped: unstructured Markdown prose is the note format (no front-matter), closing the format-stability question by observing unparsed text has nothing to destabilize; extends [ADR-0002](../03-adrs/ADR-0002-persistence-content-addressing-and-on-disk-layout.md)'s `.foundry/acts/` commit-to-git convention to `.foundry/knowledge/` by identical reasoning; and explicitly declines Derived-Knowledge indexing, semantic retrieval, provenance scoring, and formal note curation until each has a named, concrete trigger. Status: Proposed, not yet ratified.
- **2026-07-21** — Ratified [ADR-0007](../03-adrs/ADR-0007-knowledge-and-semantic-store.md) under [ADR-0000](../03-adrs/ADR-0000-governance-and-ratification-process.md), the same day it was drafted. [knowledge.md](../02-architecture/knowledge.md)'s "Unresolved: format stability" section resolved (cites this ADR); [getting-started.md](../04-guides/getting-started.md) gains a commit-`.foundry/knowledge/`-to-git recommendation mirroring its existing `.foundry/acts/` one; [roadmap.md](roadmap.md) open decision 7 marked RESOLVED; [invariants.md](../05-reference/invariants.md)'s closing note no longer lists I11 as pending; [README.md](../03-adrs/README.md)'s backlog table updated (row moved to Accepted); [RFC-0004](../01-rfcs/RFC-0004-multi-executor-router-and-publish-policy.md) §4 and [RFC-0005](../01-rfcs/RFC-0005-authored-knowledge-retrieval.md) §4's "Knowledge & semantic store" rows updated to cite this ADR as Accepted. Only "Extension isolation & contract versioning" and "Cost as a first-class constraint" remain in the ADR backlog.
- **2026-07-21** — Shipped a first M5 "change-review UI" slice, code only (no ADR needed — additive, no compatibility surface): (1) `cli.Show`/`formatAct` (`cli/cli.go`) now tints the Verdict line and the patch through `renderVerdict`/`renderDiff` exactly as the live `PromptForApproval` prompt already does — `foundry show` was previously the one Act-review surface without any coloring, and its Verdict line silently dropped the ✓/✗ symbol the live prompt already shows. (2) `formatAct` gains a new "Steps:" section rendering `domain.Act.Steps`' per-Step trace (ID, Kind, a Step-appropriate verdict via the new `cli/render.go`'s `renderStepVerdictLabel`, and duration) — data already recorded by `engine/strategy.go`'s `recordStep` but never surfaced outside live progress narration; a multi-verify Pipeline (e.g. the built-in `review` Pipeline) previously collapsed to only its last verify Step's verdict in `foundry show`'s flat fields, silently dropping an earlier Step's own findings even though the Record already carried them. Both changes are purely additive rendering over already-recorded fields — no new domain/storage fields, no schema change. Tests: `cli/format_test.go`'s `TestFormatAct_ColorTintsVerdictAndPatch` and `TestFormatAct_StepsTrace` (new); `TestFormatAct_Golden`/`TestFormatAct_UnapprovedWithoutEvidenceOrPatch`/`cli_test.go`'s `TestCLI_Show_PrintsFullAct` updated to match. Observability (structured logging/telemetry, M5's other named gap) deliberately not attempted in this pass — no concrete consumer exists yet, and this project's own ADR-0009 already declined a machine-readable output contract for the same "no consumer, no schema" reason; revisit against a real need, not speculatively.
- **2026-07-21** — Shipped a first M5 "observability" slice, code only (no ADR needed — additive, opt-in, no compatibility surface): reconsidered the prior entry's deferral once roadmap.md itself was re-read closely — unlike Derived Knowledge/semantic retrieval (ADR-0007, genuinely no named consumer), M5's own milestone definition names observability directly, which is itself the concrete, named consumer ADR-0009's "no consumer, no schema" reasoning was checking for; ADR-0009 declined a machine-readable *output* contract for `show`/`log`/`do` specifically, a different surface than a side-channel diagnostic log, so no conflict. New `engine.SlogReporter` (`engine/slog_reporter.go`) implements `engine.Reporter` by emitting structured JSON events (`act.gather.start`, `act.execute.start`, `act.verify.start`, `act.verify.done` with verdict/findings-count, `act.repair.start`, `act.repair.skipped`/`act.budget.exceeded` at Warn) via the standard library's `log/slog` — no new dependency. New `engine.MultiReporter` (`engine/reporter.go`) fans one Engine run out to more than one Reporter, the same small composition seam already established once per port by `ExecutorRegistry`/`Router`, `ApplierRegistry`, and `gatherer.Compose`. New `cli.NewReporter` (`cli/reporter.go`) is the one place both composition roots (`cmd/foundry/commands/do.go`, `session/run_pipeline_command.go`) now call instead of `cli.NewProgressReporter` directly: it returns the unchanged human-only `ProgressReporter` unless the `FOUNDRY_LOG` environment variable is set, in which case it also attaches a `SlogReporter` writing JSON lines to stderr — env var unset (the default) is byte-for-byte unchanged behavior, mirroring how every other optional behavior in this codebase (`.foundry/executors.json`, `.foundry/config.json`) defaults to off. Tests: `engine/reporter_test.go` (`MultiReporter` fan-out, including the zero-Reporters case), `engine/slog_reporter_test.go` (decodes emitted JSON lines, asserts level/msg/fields), `cli/reporter_test.go` (`NewReporter`'s env-gated wiring). Documented in [getting-started.md](../04-guides/getting-started.md)'s new "Structured logging" section. Per-Executor cost/timing telemetry (RFC-0004 §2.7) remains a distinct, larger, un-designed follow-up, not attempted here.
- **2026-07-21** — Drafted [ADR-0011](../03-adrs/ADR-0011-cost-as-a-first-class-constraint.md) (Cost as a first-class constraint), out of the backlog's own listed dependency order (ahead of ADR-0008) since it has real, shipped code to ratify/extend (`engine.CostEstimator`, `engine/budget.go`) where ADR-0008 has none yet (M6 not started, and OQ-005 itself recommends deciding nothing on extension isolation until that milestone begins). Closes [ADR-0005](../03-adrs/ADR-0005-executor-contract-and-capability-model.md) Open Question 1 with a firm "no" (`CostEstimator` stays optional — Claude Code's subprocess structurally has no billing signal, not merely "not yet"); proposes a small, genuinely additive domain change (optional `ActualCostUSD` on `Outcome`/`StepRecord`/`Act`, populated by `executor/openai` from the Chat Completions response's `usage` field it currently discards) for a follow-up implementation PR once ratified; declines to make Budget's hardcoded ceilings configurable or to build cost reconciliation/calibration until each has a named, concrete trigger. Status: Proposed, not yet ratified.
- **2026-07-21** — Ratified [ADR-0011](../03-adrs/ADR-0011-cost-as-a-first-class-constraint.md) under [ADR-0000](../03-adrs/ADR-0000-governance-and-ratification-process.md), the same day it was drafted. [roadmap.md](roadmap.md) open decision 9's cost half marked RESOLVED (its paired "near-term single-user value" half explicitly left open, not conflated); [README.md](../03-adrs/README.md)'s backlog table updated (row moved to Accepted; only "Extension isolation & contract versioning" (ADR-0008) remains). Unlike every other ADR ratified so far, this one requires a real follow-up implementation PR (Migration Strategy steps 1–5: `Outcome.ActualCostUSD`, `StepRecord`/`Act` accumulators, `executor/openai` wiring, `engine/strategy.go` wiring, `cli`/`SlogReporter` surfacing) — tracked as in-progress immediately below rather than claimed as already shipped.
- **2026-07-21** — Shipped [ADR-0011](../03-adrs/ADR-0011-cost-as-a-first-class-constraint.md)'s follow-up implementation PR (all 5 Migration Strategy steps): `domain.Outcome` gained `ActualCostUSD *float64` (reported Evidence only, never a Budget gate); `domain.StepRecord` and `domain.Act` gained matching accumulators, plus `Act.ActualCostCoverage()` (a computed helper, not a stored count, for honest "N of M generate Steps" reporting per Decision 3); `executor/openai.Executor.Execute` now decodes the Chat Completions response's `usage` object (previously received and silently discarded — `chatCompletionResponse.Usage`, `actualCostUSD`) using the same per-model price table `EstimateCostUSD` already reads; `engine/strategy.go`'s generate-Step handling accumulates it onto `act.ActualCostUSD` and threads it through `recordStep`; `engine.Reporter` gained one new additive method, `Executed(iteration int, actualCostUSD *float64)`, fired right after a successful `Execute` — `noopReporter`/`MultiReporter` fan it out, `engine.SlogReporter` emits it as `act.execute.done` (with `actual_cost_usd` only when non-nil), `cli.ProgressReporter` implements it as an intentional no-op (this is reported Evidence for `foundry show`/`FOUNDRY_LOG`, not live human narration); `cli/cli.go`'s `formatAct` renders an "Actual cost" line only when `act.ActualCostUSD != nil`, never a permanent empty placeholder. `engine/strategy.go`'s `lastOutcomeAndJudgment` (used by `Engine.Resume`) also carries `ActualCostUSD` forward from a recorded Step so resume doesn't silently drop it. Tests: `engine/reporter_test.go`, `engine/slog_reporter_test.go` (both extended), `executor/openai/openai_test.go` (`TestExecute_Success` extended plus three new tests), `cli/format_test.go` (two new tests) — all existing `fakeReporter`-asserting tests in `engine/engine_test.go` updated for the new `executed:N:none` event every existing (non-cost-reporting) test now emits. `go build ./...`, `go vet ./...`, and `go test -race ./...` all green; `gofmt -l .` clean.
- **2026-07-21** — Shipped M5's "richer review UI" follow-up named in the previous change-review-UI entry: paging for long diffs/findings. New `cli/pager.go` — `maybePage(out io.Writer, page bool, content string) error` pipes content longer than `pagerThresholdLines` (40) through `$PAGER` (default `less -R`, preserving `renderDiff`'s ANSI color) via an injectable `pagerRunner` package variable, falling back to a direct write on any pager failure so a broken `$PAGER` can never eat output. `page` is an explicit caller-decided bool (`colorEnabled(out)`), matching `renderDiff`/`renderVerdict`'s own established pattern of never re-deriving terminal-ness internally — keeps `maybePage` itself trivially testable via a fake `pagerRunner`, no real terminal or subprocess needed. Wired into `cli.PromptForApproval` (the proposed-patch section only; the verdict/prompt lines print directly) and `cli.CLI.Show`. Tests: `cli/pager_test.go` (6 new tests: threshold boundary, `page=false` never pages, pager invocation args/stdin, `$PAGER` respected, pager-failure fallback, `countLines`). No ADR required — additive CLI behavior, no compatibility surface, consistent with M5's other real-code slices this session (change-review UI, observability, actual-cost reporting). `go build ./...`, `go vet ./...`, `go test -race ./...` all green; `gofmt -l .` clean.
- **2026-07-21** — Drafted [ADR-0008](../03-adrs/ADR-0008-extension-isolation-and-contract-versioning.md) (Extension isolation & contract versioning), the last remaining ADR backlog row. Decides no isolation mechanism (in-process/out-of-process/sandboxed/tiered — all rejected as premature, no real third-party extension exists to design against) and no versioning policy (no concrete contract shape exists yet to version) — closes [OQ-005](../06-open-questions/OQ-005-extension-isolation.md) with its own "decide nothing yet" recommendation rather than overriding it. Its real, load-bearing work: corrects [ADR-0001](../03-adrs/ADR-0001-language-and-toolchain.md) clause 4 (and its downstream Consequences/"Harder"/Future-ADR-Dependencies text), which asserted "the plugin/provider contract is defined in a language-neutral wire format (gRPC/protobuf), decided in ADR-0008" as a **binding** decision — an assertion [extensibility.md](../02-architecture/extensibility.md) and [OQ-005](../06-open-questions/OQ-005-extension-isolation.md) had already withdrawn as premature, but ADR-0001's own text was never updated to match, the same class of live contradiction [ADR-0002](../03-adrs/ADR-0002-persistence-content-addressing-and-on-disk-layout.md) corrected for ADR-0001's SQLite clause. R2 (language-agnostic extension boundary) remains fully binding throughout. Names the concrete trigger for a real future mechanism decision: an actual third party wanting to write a Strategy/Executor/Validator/Context Source outside this repository. Status: Proposed, not yet ratified.
- **2026-07-21** — Ratified [ADR-0008](../03-adrs/ADR-0008-extension-isolation-and-contract-versioning.md) under [ADR-0000](../03-adrs/ADR-0000-governance-and-ratification-process.md), the same day it was drafted — **the ADR backlog is now empty.** [ADR-0001](../03-adrs/ADR-0001-language-and-toolchain.md)'s extension-boundary clause (4) and its downstream Consequences ("ADR-0008... becomes low-risk"), "Harder" (WASM hardening track, in-process sandboxing), Migration Strategy (point 2), and Future ADR Dependencies (point 4) text all corrected in place — none of them asserted a decision this project actually stands behind; [extensibility.md](../02-architecture/extensibility.md)'s "Unresolved" callout resolved (deliberate non-decision, not oversight); [OQ-005](../06-open-questions/OQ-005-extension-isolation.md) marked RESOLVED (both its own file and [open-questions/README.md](../06-open-questions/README.md)'s index); [README.md](../03-adrs/README.md)'s backlog table emptied (row moved to Accepted; ADR-0001's "pending amendment" note fully resolved, both halves). M6's row above updated to note the milestone's one policy question is now closed even though the milestone itself remains code-not-started.
- **2026-07-21** — Started a hardening/robustness pass across already-shipped code (no new architecture, no new ADR — pure error-handling and edge-case work), per the maintainer's own direction once the ADR backlog emptied. First fix: `engine/budget.go`'s `tracker.charge` now refuses a negative `estimateUSD` with a distinct, non-`ErrBudgetExceeded` error instead of silently accepting it. Without the guard, `t.costUSD+estimateUSD > t.budget.MaxCostUSD` evaluates false for a negative-enough estimate, *decrementing* `t.costUSD` and permanently loosening `MaxCostUSD`'s ceiling for every later charge in the same Act — a silent defeat of `domain.Budget`'s own contract ("enforced as a constraint, never merely reported") that a broken third-party `CostEstimator` ([ADR-0005](../03-adrs/ADR-0005-executor-contract-and-capability-model.md)) or `ActualCostUSD` ([ADR-0011](../03-adrs/ADR-0011-cost-as-a-first-class-constraint.md)) implementation could trigger, with no crash and no visible symptom until a later Act ran far over budget. New test: `engine/budget_test.go`'s `TestTracker_NegativeEstimateRefusedNotSilentlyLoosened` (asserts the charge is refused, tracker state is unchanged, and the ceiling still enforces correctly afterward). Surfaced by a targeted hardening survey covering `record/`, `gatherer/`, `workspace/`, `executor/`, and `cli/`; the remaining findings from that survey (non-atomic writes in `record.FileStore`/`record.CheckpointStore`, `record.FileStore.List` failing its whole result on a concurrent in-flight `Write`, `gatherer.NaiveGatherer` reading a full file into memory before its byte budget applies, a worktree-creation error path that skips `git worktree prune`) are real but larger/riskier changes to the durable Record and workspace-isolation mechanisms — queued for later iterations of this same pass, not attempted in one PR. `go build ./...`, `go vet ./...`, `go test -race ./...` all green; `gofmt -l .` clean.
- **2026-07-21** — Second hardening iteration: `record.FileStore.Write` (`record/store.go`) is now atomic. Previously `MkdirAll` + `O_EXCL`-open + `Write` could crash between opening and writing, leaving a permanent zero-length `act.json` — `O_EXCL` then refuses every future `Write` for that ID with `ErrAlreadyExists` (mistaking "exists but corrupt" for "already legitimately recorded"), and `Read`/`List` would hit a JSON decode error forever; the Record (I8's durable audit trail) had no way to recover. Fix: encode, write, and `Sync` to a temp file in the same directory, then publish via `os.Link` (not `os.Rename`) — `Link` fails with `EEXIST` if `act.json` already exists, preserving the exact write-once exclusivity `O_EXCL` gave, while guaranteeing any `act.json` a reader can observe is either absent or fully written, never partial. New `ErrNotFound` sentinel (mirroring `ErrAlreadyExists`) lets `List` distinguish the *benign* race its own directory scan can hit — an Act's directory exists (`MkdirAll` already ran) but `act.json` isn't published yet — from a genuine corruption/permissions problem: `List` now skips the former (every other, fully-written Act still comes back) but still propagates the latter as a real error, matching (for the right reason) `record.CheckpointStore.List`'s existing tolerance for a missing file, without adopting its blanket "swallow any error" posture, which would be unsafe for the durable Record. New tests: `TestFileStore_List_SkipsActDirectoryWithNoActJSONYet`, `TestFileStore_Write_RetriesCleanlyAfterSimulatedCrash` (a stray temp file from a simulated crash no longer blocks a retry), `TestFileStore_Write_LeavesNoStrayTempFile` (confirms normal-path cleanup); `TestFileStore_Read_NotFound` extended to assert `errors.Is(err, ErrNotFound)`. `record.CheckpointStore.Save`'s identical non-atomic-write gap (`record/checkpoint.go`) is a lower-severity version of the same issue (checkpoints are explicitly mutable/recomputable, per ADR-0002, unlike act.json) — queued for a later iteration, not fixed here to keep this PR to one mechanism. `go build ./...`, `go vet ./...`, `go test -race ./...` all green; `gofmt -l .` clean.
- **2026-07-21** — Third hardening iteration: `record.CheckpointStore.Save` (`record/checkpoint.go`) is now atomic, the same write-temp-then-publish pattern just applied to `FileStore.Write` — but publishing via `os.Rename`, not `os.Link`, since a checkpoint is explicitly meant to be overwritable (unlike the write-once Record), and `Rename` atomically replaces an existing destination on POSIX where `Link` would instead fail with `EEXIST`. Previously a crash between opening and writing left a truncated `checkpoint.json` — corrupting the exact mechanism `foundry resume` depends on to recover from a crash, the same failure mode `FileStore.Write` had for the Record itself. New tests: `TestCheckpointStore_Save_RetriesCleanlyAfterSimulatedCrash`, `TestCheckpointStore_Save_LeavesNoStrayTempFile`. All of this session's ADR-0007/ADR-0011/ADR-0008 ratification work and the M5 change-review-UI/observability/actual-cost/paging features were committed in 4 logical commits (`docs(adrs)`, `feat(cli)`, `feat(engine)`, `fix(engine,record)`) before this hardening pass continued. `go build ./...`, `go vet ./...`, `go test -race ./...` all green; `gofmt -l .` clean.
- **2026-07-22** — Fourth hardening iteration (running unsupervised overnight, committing each green iteration per the maintainer's own direction): `gatherer.NaiveGatherer` (`gatherer/gatherer.go`) no longer reads an entire file into memory before its `maxContextBytes` budget applies. New `readBounded(path, limit)` opens the file and reads at most `limit+1` bytes via `io.LimitReader`, replacing all four `os.ReadFile` call sites (the named-file phase, the identifier-fallback phase, the supplementary phase, and `findByIdentifier`'s own directory-walk scan). Previously a large binary/log/database dump a repository happens to track — named directly by an Intent, or encountered by the identifier fallback's walk — was read in full regardless of size, an unbounded memory spike despite `maxContextBytes` (100 KiB) advertising a small cap; `findByIdentifier`'s own `len(content) > maxContextBytes` check (already present) ran only *after* that full read. The bound is applied to the overall budget ceiling, not threaded through each call site's own shrinking `remaining` value — a deliberately smaller, safer fix than fully precise per-call bounding would have required. New tests: `TestReadBounded_NeverReadsPastLimitPlusOne` (a file 50x over a small limit still yields exactly `limit+1` bytes), `TestReadBounded_ReturnsFullContentWhenUnderLimit`, `TestReadBounded_MissingFileReturnsError`; all pre-existing `gatherer` tests (`TestGather_BoundsTotalOutput` et al.) still pass unchanged. `go build ./...`, `go vet ./...`, `go test -race ./...` all green; `gofmt -l .` clean.
- **2026-07-22** — Fifth hardening iteration: `workspace.NewWorkspace`'s (`workspace/workspace.go`) error path now attempts `git worktree prune` after a failed `git worktree add`, mirroring `cleanup()`'s own existing fallback for exactly the scenario its comment already documents — a killed process (or here, a disk-full error partway through populating the worktree) can leave `.git/worktrees/<name>` registered after `os.RemoveAll(tmpDir)` has already removed its directory. `prune` only ever removes a registration whose directory no longer exists, so it is always safe to attempt best-effort, even when `worktree add` failed for an unrelated reason. Deliberately does **not** also delete `branchName` in this path (unlike `cleanup()`'s own full teardown): if `worktree add -b branchName` failed because a branch of that name already existed, that branch predates this call and was not created by it — destroying it in an error-cleanup path would be a real, newly-introduced correctness bug, not a fix; caught and corrected before landing, during this same iteration, by reasoning through the "branch name collision" failure mode specifically. New test: `TestNewWorkspace_FailedWorktreeAddPrunesWithoutTouchingExistingBranch` (pre-creates a same-named branch, triggers the failure, asserts the branch survives and no stray worktree registration remains). `go build ./...`, `go vet ./...`, `go test -race ./...` all green; `gofmt -l .` clean.
- **2026-07-22** — Sixth hardening iteration, and the first genuine security-shaped gap this pass found: `workspace.ProjectDocApplier.Apply` (`workspace/knowledge_applier.go`) now refuses a `DocsPath` that is absolute or escapes the project root, via new `resolveWithinRoot` (mirroring `gatherer.NaiveGatherer.resolve`'s existing boundary check exactly). Previously `DocsPath` — sourced verbatim from `.foundry/config.json`'s `docs_path` with zero validation anywhere in `project.LoadConfig` — was joined directly into a filesystem path with no traversal check: a value like `"../../../../tmp/evil.md"` let a Pipeline's `project-doc` apply Step write outside the project directory entirely. New tests: `TestProjectDocApplier_RefusesPathTraversal` (two `../`-style values, plus a check that no file lands outside the project root), `TestProjectDocApplier_RefusesAbsolutePath`. Surfaced by a second hardening survey covering `session/`, `vcs/`, `verify/`, `project/`, and `replay/`; two more real findings from that survey — a REPL slash-command dispatch loop with no panic recovery (`session/repl.go`), and `vcs.GitHubPRApplier.Apply` leaving a pushed branch dangling with no discoverable state if `gh pr create` fails after the push already succeeded (`vcs/github_pr_applier.go`) — are queued for later iterations, not attempted in this same PR. `go build ./...`, `go vet ./...`, `go test -race ./...` all green; `gofmt -l .` clean.
- **2026-07-22** — Seventh hardening iteration: `session.REPL`'s dispatch loop (`session/repl.go`) now recovers from a panic anywhere in a slash command's call chain (`RunPipelineCommand` → `cli.CLI.Do` → `Engine` → `Gatherer`/`Executor`/`Verifier`/`Applier`) instead of crashing the whole interactive session. `Run`'s own doc comment already promised "one failed slash command must never end the session," but that guarantee held only for a *returned* error — a panic bypassed it entirely. New `dispatchRecovered` wraps `CommandRegistry.Dispatch` in a `recover()`, converting a panic into a rendered error the same way any other command failure already renders, while still writing the panic's full value and stack trace to stderr via `runtime/debug.Stack()` — a real programming bug stays loud and debuggable; only the user-facing session survives it. New test: `TestREPL_PanicInCommandDoesNotStopSession` (a fake `CommandHandler` that always panics; confirms the session reports the panic and a *subsequent* command still runs normally). `go build ./...`, `go vet ./...`, `go test -race ./...` all green; `gofmt -l .` clean.
- **2026-07-22** — Eighth hardening iteration, closing out the second survey's queue: `vcs.GitHubPRApplier.Apply` (`vcs/github_pr_applier.go`) now names the dangling state explicitly when `gh pr create` fails *after* `Push` already succeeded — previously the error read identically to any other `gh` failure, giving no hint that a real branch was already live on the remote with no PR open for it. The error now names the branch, the remote, and a concrete recovery command (`gh pr create --head <branch>`); `Clean` was already (correctly) not called on this path, so the local worktree also survives for retry — only the error message was silent about why. New test: `TestGitHubPRApplier_Apply_GHFailureAfterPushNamesTheDanglingBranch` (asserts the error names the branch, and independently confirms via `git branch --list` against the bare test remote that the branch really is there — proving the error's claim, not just its wording). This closes the second hardening survey's full queue (panic recovery, `docs_path` traversal, and this dangling-branch message). `go build ./...`, `go vet ./...`, `go test -race ./...` all green; `gofmt -l .` clean.
- **2026-07-22** — Ninth hardening iteration, and the most significant finding of this entire overnight pass, surfaced by a third, genuinely final survey: **the interactive session's Acts could never be resumed at all.** `session.NewSession` (`session/session.go`) never held a `record.CheckpointStore`, and `session.RunPipelineCommand.Run` (`session/run_pipeline_command.go`) never called `engine.Engine.SetCheckpointSaver` — unlike `cmd/foundry/commands/do.go`'s `wireEngine`, which always wired one. Since `engine.NewEngine` defaults `checkpoints` to a no-op saver, **no interactive Act ever wrote a `checkpoint.json`**, regardless of how far into a Pipeline it got. Since [ADR-0009](../03-adrs/ADR-0009-cli-and-output-contract.md) made the interactive session Foundry's *primary* interface, this was the common case, not an edge case: kill a session mid-`/feature` and `foundry resume` reported "No interrupted acts" — the in-progress work simply gone, no error, nothing to recover. Fix: `Session` now constructs and holds a `*record.CheckpointStore` (same `.foundry/acts` root `record.Recorder` already uses) via a new `Checkpoints()` accessor, mirroring `Recorder()`; `RunPipelineCommand.Run` now calls `eng.SetCheckpointSaver(s.Checkpoints())` alongside its existing `SetCheckpointer`.
  A second, compounding bug in the same capability: even with a checkpoint saved, `cmd/foundry/commands/resume.go`'s `wireEngine` resolved `pipelineName` from `engine.NewDefaultRegistry()` — Foundry's two built-in Pipelines only ("default", "review"). `foundry do` itself never needed more (it always asks for "default"), but a checkpoint left by an interactive session names whatever Pipeline the session ran — "feature", "bugfix", "release" (the exact starters `project.ProjectLoader.Scaffold` writes), or any project-authored Pipeline. Resuming one of those failed with "pipeline not registered" even with a valid checkpoint present. `wireEngine` now resolves from `project.ProjectLoader.LoadRegistry` (built-in plus project-local) instead — a strict superset, so `foundry do`'s own hardcoded `"default"` resolution is unaffected; `wireEngine` gained a `ctx context.Context` parameter to support this, threaded through both its callers (`Do`, `Resume`).
  New tests: `session/run_pipeline_command_test.go`'s `TestRunPipelineCommand_SavesCheckpointOnInterruption` (a custom Executor that fails to apply on attempt 1 — a legitimate verdict, not an error — then returns a genuine Go error on the repair round `default.json` allows, deterministically leaving a checkpoint on disk with no timing dependency); `cmd/foundry/commands/do_test.go`'s `TestWireEngine_ResolvesProjectLocalPipeline` (resolves all five Pipeline names — two built-in, three scaffolded starters — against a freshly-scaffolded project). `go build ./...`, `go vet ./...`, `go test -race ./...` all green; `gofmt -l .` clean.
  Ten hardening commits total this session, on top of four ADR/feature commits from earlier in the session (14 commits total, none pushed). A third survey pass (`project/`, `verify/`, `executor/claude/`, `cmd/`, `replay/`, remaining `engine/`) found this one real gap and nothing else — every other area checked out clean.
- **2026-07-22** — Eleventh hardening iteration, found by **live, real-Executor validation** of the resume fix above (a genuine end-to-end run against Claude Code — not simulated), not by static code survey: `engine/strategy.go`'s `Produce` and `engine/engine.go`'s `Resume` both used to set `act.JudgmentVerdict`/`act.CheckedFindings` only *after* `runSteps` returned — but a Pipeline declaring its own `record` Step (RFC-0002 §9 Phase 4 — the exact shape `.foundry/pipelines/feature.json` ships, and this repo's own dogfooded one) calls `Checkpointer.Write` *earlier*, inside that same `runSteps` call. Every Act produced by such a Pipeline was therefore persisted with a permanently empty `JudgmentVerdict` and nil `CheckedFindings` — confirmed live: `foundry show` on a real resumed-and-approved Act displayed `Verdict:    ✗ ` (empty) and `Checked evidence: (none)`, despite the verify Step's own recorded `StepRecord` correctly showing `✓ pass` with real `go-build`/`go-test` findings the whole time. Fixed by setting `act`'s flat fields inside `runSteps`' own `verify` case, the moment the last verify Step actually produces a `Judgment`, removing the now-redundant post-hoc assignments in both `Produce` and `Resume`. New test: `engine/pipeline_golden_test.go`'s `TestGoldenFeaturePipeline_RecordStepSeesJudgmentAlreadySet`, using a new `snapshotCheckpointer` fake that captures the fields' value *at Write-call-time* rather than the act pointer itself — confirmed by temporarily reverting the fix that the test fails exactly as expected (`Checkpointer.Write saw JudgmentVerdict = ""`) and passes with it restored. Named honestly: the existing `fakeCheckpointer` (`engine/strategy_test.go`) stores the `*domain.Act` pointer, not a snapshot, so any later mutation to the same struct is retroactively visible once a test inspects it after `Run` returns — a real blind spot in that fixture, which is exactly why this bug went uncaught by the extensive existing golden-pipeline test until a live run surfaced it.
  Eleven hardening commits total this session (fifteen including the four ADR/feature commits), none pushed. Live validation performed: a real interactive session (`/init`, `/feature` with a project-authored `generate → verify → approve → apply → record` Pipeline) against a real Claude Code Executor, killed with `kill -9` at the live approval prompt, confirmed a real `checkpoint.json` survived naming `"pipeline": "feature"`; `foundry resume` (list mode) found it; `foundry resume <id>` resumed it, re-presented the *same* recorded patch (no second Executor call), accepted approval, applied the patch to the real working tree, and recorded a real `act.json` — proving both halves of the resume fix (checkpoint saving and project-local Pipeline resolution) end to end, which is what surfaced this eleventh bug in turn. `go build ./...`, `go vet ./...`, `go test -race ./...` all green; `gofmt -l .` clean.
- **2026-07-22** — Investigated M3/M4's remaining non-declined scope (everything not already gated by ADR-0006/0007/0011's named-trigger deferrals). **M4 has no real gap left**: traced provenance end to end — `knowledge.Gatherer.Gather` attributes each retrieved note inline (`<path>:\n<content>`), flows unchanged through `engine.Engine.RunBudgeted` into `act.ConsideredFiles`, into both `executor/openai` and `executor/claude`'s prompts, and is rendered by `cli.go`'s `formatAct` under "Considered evidence" — attribution genuinely survives note → prompt → persisted Act → Authority's Judgment view. Confirmed `engine/strategy.go`'s apply-Step approval check (`act.ApprovedAt != nil`) applies to every apply Target, including `workspace.KnowledgeNoteApplier`, uniformly — no code path writes a knowledge note without a preceding accepted approve Step. M4 stays "Partial" in the table above solely because of ADR-0007's deliberately-declined items (Derived Knowledge, semantic retrieval, provenance scoring), not because of any buildable-today gap. **M3 had one real gap**: `replay.Verify` had only ever been exercised against hand-built fixture patches (`replay/replay_test.go`) — no test or runbook confirmed it against a patch a real model actually produced, which is exactly where a real diff's fuzzy context/whitespace could break `git apply` on replay in a way no fixture would catch. Closed by `replay/replay_live_test.go`'s `TestVerify_ReproducesARealClaudeCodeProducedAct` (skipped by default, `FOUNDRY_LIVE_TEST=1`-gated so it never runs in normal `go test ./...` or CI without a real, authenticated `claude` CLI) — run live in this environment: passed, confirming a real Claude-Code-produced patch survives `StagedVerifier`'s `git apply` and re-verifies identically on replay. `go build ./...`, `go vet ./...`, `go test -race ./...` all green; `gofmt -l .` clean.
- **2026-07-22** — Drafted [ADR-0012](../03-adrs/ADR-0012-interactive-terminal-ux-and-first-dependency.md) (Interactive terminal UX & Foundry's first third-party dependency), per the maintainer's direct request for an autocomplete-and-suggestions terminal experience closer to Claude Code/Codex/OpenCode's own CLIs. Proposes `charmbracelet/bubbletea`/`bubbles`/`lipgloss` — pure Go, no cgo, R1-compatible — confined strictly to `session/`+`cli/`, replacing `session/repl.go`'s plain `bufio.Reader.ReadString('\n')` read loop with a raw-mode line editor while leaving `CommandRegistry.Dispatch`/`ParseLine`/every domain-engine port untouched. This is Foundry's first-ever external Go dependency (`go.mod` had no `require` block before this) — the exact "dependency surface grows" trigger [ADR-0001](../03-adrs/ADR-0001-language-and-toolchain.md) itself flagged as a future, undecided policy question. **Ratified the same day** once the maintainer confirmed the dependency choice directly.
- **2026-07-22** — Implemented ADR-0012's v1 slice. Added `cli.IsInteractiveTerminal(in, out)` (`cli/render.go`), reusing `colorEnabled`'s exact `*os.File`-character-device check on both sides — a redirected/piped/test `io.Reader`/`io.Writer` (every existing test's `strings.Reader`/`bytes.Buffer`) is never "interactive," so none of them change behavior. `session.Session` gained an `Interactive bool` field, computed once in `NewSession` from the original `in`/`out` before `In` is wrapped in `*bufio.Reader`. New `cli.ReadInteractiveLine` (`cli/interactive_prompt.go`) wraps a `bubbles/textinput` model in a `bubbletea` program: Tab-completion via `textinput`'s built-in `ShowSuggestions`/`SetSuggestions` (inline "ghost" suggestions filtered by prefix, styled via `lipgloss`), plus a new `PromptHistory` type for Up/Down in-session recall — intercepted in `promptModel.Update` *before* forwarding to `textinput.Update`, since `textinput`'s own default `KeyMap` binds Up/Down to *suggestion* cycling, not history, and this avoids that conflict entirely rather than reconfiguring the library's keymap. The program owns the terminal for exactly one line — `bubbletea` restores cooked mode before `Run` returns — so `cli.PromptForApproval`'s own later, unrelated blocking reads from the same `*bufio.Reader` are completely unaffected. `session/repl.go`'s `REPL.Run` now calls a new `readLine()` that branches on `session.Interactive`: true gets the rich editor; false (every existing test) gets the exact unmodified `bufio.Reader.ReadString('\n')` path, so the entire existing REPL test suite required zero changes. New tests: `cli/interactive_prompt_test.go` (`PromptHistory` add/recall/nil-safety, `promptModel`'s Enter/Ctrl-C/history-browsing transitions, exercised directly against `Update` with no real terminal needed), `cli/render_test.go`'s `TestIsInteractiveTerminal_FalseForNonFileIO`, `session/session_test.go`'s `TestNewSession_NotInteractiveForNonFileIO`. R1 re-confirmed: `CGO_ENABLED=0 go build ./cmd/foundry` still produces a static binary. Confirmed via `grep` that no import of `bubbletea`/`bubbles`/`lipgloss` exists outside `cli/`. **Validated live** in a real pty (Python's `pty` module driving the actual `foundry` binary, answering its startup terminal-capability queries — OSC 11 background-color and DSR cursor-position — the way a real terminal emulator would): typing `/he` renders the inline `/help` ghost suggestion; Tab accepts it; Enter dispatches through the unmodified `CommandRegistry.Dispatch`, producing real `/help` output; Up recalls `/help` from history; a second Enter re-dispatches it; `/exit` ends the process cleanly (`os.waitpid` confirmed exit status 0, no kill needed). `go build ./...`, `go vet ./...`, `go test -race ./...` all green; `gofmt -l .` clean.
- **2026-07-22** — Implemented ADR-0012's v2 slice, after the maintainer shared a reference screenshot of Claude Code's own startup panel and asked for the same style plus a full, navigable "/" menu (not just single-suggestion completion). Replaced `bubbles/textinput`'s inline ghost-suggestion mechanism entirely with a full dropdown (`cli/interactive_prompt.go`'s `promptModel.menu`/`filtered`/`menuCursor`/`menuOpen`): opens the instant the line starts with `/`, lists every candidate (`session/repl.go`'s new `candidates()`, returning `[]cli.CommandCandidate{Name, Description}` built from `CommandRegistry.List()` plus `/exit`/`/quit`) whose name has the current input as a prefix, Up/Down move a highlighted cursor through the filtered list (wrapping at either end), Tab or Enter accepts the highlighted candidate (completes to `"<name> "`, closes the menu, does **not** submit), and Esc closes the menu without touching the input. New `cli/interactive_renderer.go` `Banner(root, initialized bool)` replaces the old one-line banner with a `lipgloss`-bordered panel: title (`⚒ Foundry`) plus `cli.BuildVersion()` (new `cli/version.go`, reading the binary's real embedded `vcs.revision`/`vcs.modified` via `runtime/debug.ReadBuildInfo` — deliberately not a fabricated semantic version, since Foundry has no release process yet), the real project root, and whether `/init` has already run (`session.Session.Initialized()`, new — checks `project.PipelinesDir` on disk, the same marker the existing end-to-end tests already checked for after `/init`). A non-terminal `Out` (every existing test) gets the same three facts as three plain lines, no border, no color — `TestInteractiveRenderer_Banner`/`_Initialized` pin both branches.

  **A real bug a live pty run caught and this same PR fixes**: the first version of the exact-match logic let the menu stay open even once the user had typed a command's full, unambiguous name (e.g. `/exit`, which no other candidate has as a proper prefix) — so Enter's first press only "accepted" a selection that was already fully spelled out (appending a redundant trailing space) instead of submitting, forcing a confusing second Enter for every single command typed rather than arrow-selected. Fixed by closing the menu the moment the current input exactly equals any candidate's name, not only when the line no longer looks like a bare command (space, or lost its leading `/`) — regression test `TestPromptModel_ExactMatchClosesMenuSoOneEnterSubmits`.

  New tests: `cli/interactive_prompt_test.go`'s `TestPromptModel_MenuOpensOnSlashAndFiltersByPrefix`, `_MenuClosesOnSpaceOrNoLeadingSlash`, `_MenuNavigationAndAccept`, `_ExactMatchClosesMenuSoOneEnterSubmits`, `_EscClosesMenuWithoutClearingInput`; `session/session_test.go`'s `TestSession_Initialized`. Updated `cmd/foundry/main_test.go`'s `TestRun_NoArgs_StartsInteractiveSession` (asserted the old banner's literal wording). **Validated live** in a real pty: the bordered panel renders with real version/path/status; typing `/` opens all 8 candidates with the first highlighted; Down moves the highlight through `/init` → `/feature` → `/bug`; Enter on a highlighted-but-partial selection completes to `"/bug "` and closes the menu without dispatching (confirmed via the real `session: /bugfix requires a description of the work` error on the *second* Enter, proving genuine dispatch through the unmodified handler); after the exact-match fix, typing `/exit` in full and pressing Enter *once* ends the process immediately (`os.waitpid` confirmed exit status 0). `go build ./...`, `go vet ./...`, `go test -race ./...` all green; `gofmt -l .` clean.
- **2026-07-22** — Implemented ADR-0012's v3 slice: persistent, cross-session history, the increment the maintainer asked for next after v2. `cli.PromptHistory` gained `LoadPromptHistory(path string) *PromptHistory` (`cli/interactive_prompt.go`), pre-populating from a file's existing lines and remembering the path so `Add` also appends every new submission back to it immediately — append-only, so a crash mid-session loses at most nothing already flushed; a missing or unwritable file/directory degrades to an empty, still-usable history rather than a fatal error, since history persistence is convenience state, never a correctness path. `NewPromptHistory` (in-memory only) is unchanged for any caller that doesn't want a file. New `session.HistoryFile` constant (`session/repl.go`, `".foundry/history"`), wired into `NewREPL`; documented in [getting-started.md](../04-guides/getting-started.md) as something to gitignore, unlike `.foundry/acts`/`.foundry/pipelines`/`.foundry/knowledge` (this one is personal, ephemeral convenience state, not audit-worthy Evidence or Knowledge). New tests: `cli/interactive_prompt_test.go`'s `TestLoadPromptHistory_MissingFileIsEmpty`, `_ReadsExistingLines`, `TestPromptHistory_AddPersistsAcrossLoads`, `TestNewPromptHistory_AddDoesNotPersist`. **Validated live** across two separate real pty process runs against the same repo: session 1 submitted `/help`, `/bogus`, `/exit`, writing exactly those three lines to `.foundry/history` (confirmed by reading the file directly); a fresh session 2 process loaded it and Up-arrow recalled `/exit` then `/bogus`, most-recent-first, exactly as the file's contents predicted. `go build ./...`, `go vet ./...`, `go test -race ./...` all green; `gofmt -l .` clean.
- **2026-07-22** — Implemented ADR-0012's v4 slice: restyled `cli/progress.go`'s live narration with `lipgloss` (`progressActionStyle`/`progressRepairStyle`/`progressErrorStyle`/`progressDimStyle`), closing the last visual inconsistency between the interactive session's chrome (banner, "/" menu) and its plain-ANSI progress lines — the maintainer's own framing for this step. `ProgressReporter.line`'s signature changed from a raw `code string` to a `lipgloss.Style`, `.Render()`-ing only when `p.color` is true (unchanged gate, unchanged text content — every existing `progress_test.go` assertion needed no changes). Deliberately scoped to this one file, not `render.go`'s `renderVerdict`/`renderDiff`/`renderStepVerdictLabel`/`formatReplayResult`: those are shared by `foundry show`/`replay`, pinned by existing byte-exact golden tests (`render_test.go`), and restyling them would be a separate, real-regression-risk change for zero functional benefit — this file's own narration had no such tests, making it the safe, contained target. New test `TestProgressReporter_StylesAreNotNoOps` checks each style's own declared properties (`Style.GetBold`/`GetForeground` — pure data, no renderer/TTY involved) rather than rendered output, since `lipgloss.Style.Render` is itself color-profile-aware and correctly renders plain once its renderer detects no real terminal (exactly what `go test` looks like) — confirmed the hard way: forcing an explicit ANSI profile on a scratch `lipgloss.Renderer` still rendered plain, because lipgloss additionally gates on TTY-ness, not just the configured profile, before ever emitting an escape. **Validated live** against a real `foundry do` run (a genuine Claude Code call, not simulated): `Gathering`/`Executing`/`Verifying` render bold cyan (`\x1b[1;36m`), the diff and verdict rendering (untouched `render.go`) look exactly as before, confirming both halves — the new styling and the deliberately-unchanged shared helpers — coexist correctly. `go build ./...`, `go vet ./...`, `go test -race ./...` all green; `gofmt -l .` clean.
- **2026-07-23** — Researched then added `executor/gemini`, per the maintainer's request to support cheap/free-tier vendors for delegating specific tasks. Research (dated July 2026) found: GitHub Copilot's scriptable surface is real (`gh pr edit --add-reviewer @copilot`, GA 2026-03-11; Copilot CLI's `-p` headless mode, GA 2026-02-25) but paid (no free tier) and maps to a **Verifier/review-adjacent** port, not `Executor` — deferred, see below. Gemini has both a headless CLI and a direct REST API, a genuinely free tier (Flash models, no credit card), and maps cleanly onto the **Executor** port — the same HTTP-API shape `executor/openai` already established. Cursor has no documented diff/patch contract and unsettled, conflicting pricing across sources — explicitly deferred, the same "no real trigger yet" treatment ADR-0008 gives a speculative extension mechanism.

  `executor/gemini` (new package) calls Gemini's `generateContent` REST API (`ai.google.dev/api/generate-content`), authenticating via the `x-goog-api-key` header (keeps the credential out of the URL, unlike the documented `?key=` query-parameter alternative) rather than OpenAI's message-array shape — `contents`/`parts`/`systemInstruction` request, `candidates`/`content`/`parts`/`usageMetadata` response. Implements `engine.Executor` and `engine.CostEstimator` (a blended, simple-averaged per-model price table from Gemini's real July-2026 pricing page, the same "rough estimate, not billing data" caveat `executor/openai`'s own table carries). No new architectural decision needed: ADR-0005's Executor contract and ADR-0006's explicit-pin routing already cover any number of named vendors — `cmd/foundry/main.go`'s `namedExecutor` dispatch gained a `"gemini"` case alongside `"openai"`. New tests mirror `executor/openai`'s own suite structure exactly (`executor/gemini/gemini_test.go`): success, transport error, 401/429/5xx, malformed error body, no-candidates, no-diff-in-content, cost estimation (known/unknown model, considered-context sensitivity), actual-cost-from-usage (unknown-model fallback, zero-usage-stays-nil). `cmd/foundry/named_executor_test.go` gained `TestNamedExecutor_GeminiVendorConstructsGeminiExecutor`, mirroring its existing OpenAI test. Documented in [getting-started.md](../04-guides/getting-started.md) (executors.json example, free-tier note). **Not validated live**: this environment has no `GEMINI_API_KEY`, the same honest limitation `executor/openai`'s own test file already documents for OpenAI — fixture-tested against Gemini's documented response shape only, not a captured live transcript. `go build ./...`, `go vet ./...`, `go test -race ./...` all green; `gofmt -l .` clean.
- **2026-07-23** — Added `executor/geminicli`, and made it — not `executor/gemini`'s raw HTTP API key — the vendor `"gemini"` resolves to. The maintainer's own framing, immediately after `executor/gemini` shipped: a raw API key should be the *last resort*, not the default, and Foundry already has the better pattern proven twice over (`executor/claude`'s Claude Code CLI, `vcs.GitHubPRApplier`'s `gh` CLI — both delegate auth entirely to the vendor's own already-authenticated CLI, reading no credential themselves). Researched the Gemini CLI's own auth docs (geminicli.com/docs/get-started/authentication) before committing to this: "Sign in with Google" needs a browser for its *first* run only; the CLI caches that login to disk and — confirmed via the CLI's own docs and a corroborating community search — reuses it for every later *headless*, non-interactive invocation with no browser needed again. `executor/geminicli.Executor` (mirrors `executor/claude.ClaudeExecutor`'s structure exactly: `runner` interface, `execRunner`, workspace fixed at construction, prompt piped via stdin rather than a `-p` argument to avoid any `ARG_MAX` risk with a large gathered-context prompt) shells out to `gemini --output-format json` and decodes the documented `{response, error}` JSON shape, with a defensive fallback that extracts the JSON object from the first `{` to the last `}` in case a diagnostic line (e.g. "Loaded cached credentials.") precedes it on stdout — unconfirmed for JSON mode specifically, cheap to tolerate regardless.

  This forced one real, small architectural extension: `project.ExecutorConstructor`'s signature gained a `workspace string` parameter (`func(cfg ExecutorConfig, workspace string) (engine.Executor, error)`) — every named vendor before this one was a stateless HTTP call needing no directory context; a subprocess-based named vendor genuinely needs to know where to run, the same way `executor/claude`'s own top-level default-Executor factory already does. `project.BuildExecutorRegistry` now threads its own `root` parameter into `construct` as that workspace (verified to already equal the same `repoPath`/`root` every real call site passes to the default Executor's own factory — no behavior change for the vendors that don't need it, just an added parameter they ignore). No new ADR needed — `project.ExecutorConstructor` is a Go-level implementation seam ADR-0005 never named or froze the signature of. Ripple was smaller than first assumed: `cmd/foundry/commands/do.go`/`resume.go` pass the constructor function through unchanged and needed no edits; only `project/executor_config.go`, `cmd/foundry/main.go`'s `namedExecutor`, and the three test files constructing a `project.ExecutorConstructor` by hand needed updating.

  `cmd/foundry/main.go`'s `namedExecutor` now resolves `"gemini"` → `executor/geminicli`, `"gemini-api"` → `executor/gemini` (renamed from bare `"gemini"`), `"openai"` unchanged. New tests: `executor/geminicli/geminicli_test.go` (mirrors `executor/claude`'s own suite: success with args/stdin captured, model flag appended only when set, tolerating diagnostic noise before the JSON blob, missing executable, timeout, failure with/without stderr/stdout content, a CLI-reported JSON `error` field, empty response, no-diff-in-response, fully unparseable output, no workspace configured); `cmd/foundry/named_executor_test.go` renamed its Gemini test to `TestNamedExecutor_GeminiVendorConstructsGeminiCLIExecutor` and added `TestNamedExecutor_GeminiAPIVendorConstructsGeminiExecutor`; `project/executor_config_test.go` and `session/executor_registry_wiring_test.go` updated their hand-written `construct` funcs to the new two-argument signature (one gained a workspace-received assertion). Documented in [getting-started.md](../04-guides/getting-started.md): requirements table gains a Gemini CLI row ahead of the API-key row, and the `executors.json` example shows both `"gemini"` (no `api_key_env`) and `"gemini-api"` (with one) side by side. **Not validated live**: no `gemini` CLI installed and no Google account available in this environment — fixture/mock-tested against the CLI's own documented `--output-format json` shape only, the same honest limitation the HTTP-based Gemini/OpenAI Executors already carry for their own vendors. `go build ./...`, `go vet ./...`, `go test -race ./...` all green; `gofmt -l .` clean.
- **2026-07-23** — Added Copilot code review delegation, the second half of the original two-item plan (Gemini, then Copilot review) from the maintainer's platform-support research. `vcs.GitHubPRApplier` gained `RequestCopilotReview bool` (wired from a new `project.Config.RequestCopilotReview` / `.foundry/config.json`'s `request_copilot_review`, mirroring `RequireApprovalBeforeRemotePublish`'s own opt-in pattern): after `Apply` successfully opens a pull request, it also runs `gh pr edit <branch> --add-reviewer @copilot` — the GA-since-2026-03-11 mechanism the earlier platform research identified, authenticating the same way `createPullRequest` already does (`GH_TOKEN` set only for that one subprocess call). Deliberately best-effort: a failure to request the review (no paid Copilot plan, the feature not enabled on the repo) is written to `Out` as a warning and does **not** fail `Apply` — the pull request, Foundry's own terminal action (ADR-0010 Decision 5), has already, successfully, been opened by that point, and a supplementary nice-to-have failing must not make Foundry treat the whole Act as failed. No new ADR needed: this is a small, additive extension of an already-ratified apply target (ADR-0010), not a new trust-model concept. New tests (`vcs/github_pr_applier_test.go`): `TestGitHubPRApplier_Apply_RequestsCopilotReviewWhenEnabled` (asserts the exact second `gh` call and its args/env), `_DoesNotRequestReviewByDefault` (confirms the zero value opts out, exactly one `gh` call), `_CopilotReviewFailureDoesNotFailApply` (confirms Apply still succeeds, the warning appears in `Out`, and the local branch is still cleaned up). Documented in [getting-started.md](../04-guides/getting-started.md)'s `.foundry/config.json` section. `go build ./...`, `go vet ./...`, `go test -race ./...` all green; `gofmt -l .` clean.
- **2026-07-23** — Researched further free/cheap Executor candidates the maintainer asked about (Ollama, GitHub Models, Groq, DeepSeek, Mistral, Amazon Q Developer, briefly OpenRouter/xAI/Qwen — dated July 2026 findings). Recommended and added a general `"openai-compatible"` vendor instead of a near-duplicate package per candidate: `executor/openai` gained `NewExecutorWithEndpoint(model, apiKey, endpoint string) *Executor` (`NewExecutor` now just calls it with `defaultEndpoint`) — Ollama (fully local, free, no key — an empty `apiKey` still sends a harmless empty `Authorization: Bearer ` header), Groq, DeepSeek, and GitHub Models (reusing the same `GH_TOKEN`-class credential `remote_publish_token_env` already needs) all document an explicit Chat-Completions-compatible endpoint, so this one client already covers every one of them. `project.ExecutorConfig` gained `BaseURL string` (`base_url`), required for this vendor and ignored by every other one; `cmd/foundry/main.go`'s `namedExecutor` gained an `"openai-compatible"` case that refuses construction with a clear, named error if `base_url` is missing. **Amazon Q Developer researched and explicitly declined**: AWS announced its end-of-support in 2026 — new signups, including its free tier, have been blocked since May 2026, with a full sunset already scheduled — not something to build against. Mistral's free "Codestral" tier was noted as a reasonable future addition (same `openai-compatible` path would cover it) but not added this pass, since its rate limits weren't confirmed with the same confidence as the four vendors above. New tests: `executor/openai/openai_test.go`'s `TestNewExecutor_UsesDefaultEndpoint`, `TestNewExecutorWithEndpoint_UsesGivenEndpoint` (asserts the real request URL), `_EmptyAPIKeyStillSendsAuthHeader`; `project/executor_config_test.go`'s `TestLoadExecutorConfig_DecodesBaseURL`; `cmd/foundry/named_executor_test.go`'s `TestNamedExecutor_OpenAICompatibleVendorConstructsOpenAIExecutor`, `_RequiresBaseURL`. Documented in [getting-started.md](../04-guides/getting-started.md) with a provider table (Ollama/Groq/DeepSeek/GitHub Models, each with its `base_url`) and the Amazon Q Developer decision named explicitly. **Not validated live**: no `ollama` installed or running in this environment (`curl localhost:11434` confirmed nothing listening) — fixture-tested against the documented Chat Completions shape only, the same honest limitation every other HTTP-based Executor in this repo already carries. `go build ./...`, `go vet ./...`, `go test -race ./...` all green; `gofmt -l .` clean.
- **2026-07-23** — Added `executor/copilotcli`, per the maintainer's own observation that Copilot's usefulness isn't limited to PR review (`vcs.GitHubPRApplier.RequestCopilotReview`) — it could be delegated actual generate Steps too. Fetched the GitHub Docs page on running the Copilot CLI programmatically to confirm the real invocation shape before building anything: the `copilot` binary (GA 2026-02-25), `-p`/piped-stdin for a prompt, `-s` (silent, clean text output — no documented JSON mode, unlike the Gemini CLI), `--no-ask-user` to prevent blocking on a clarifying question with nothing running headlessly to answer it, and `COPILOT_GITHUB_TOKEN` (or a reused interactive `gh auth login` session) for auth — Foundry reads no key of its own, the same PIC-2 pattern `executor/claude`/`executor/geminicli` already establish.

  **A real, new safety consideration this Executor is the first to need**: unlike Claude Code and the Gemini CLI (both already proven, live, to stay text-only when prompted to "respond with only a diff"), the Copilot CLI is documented as genuinely agentic — it ships its own file/shell tools, with `--allow-tool`/`--allow-all-tools` flags to grant them (the docs themselves warn "avoid `--allow-all` unless in sandboxed environments"). Foundry's whole trust model depends on a generate Step only ever *proposing* an Outcome for later human approval (docs/02-architecture/trust.md, Accountability) — an Executor able to mutate the workspace directly, bypassing that gate, would be a real violation, not a cosmetic one. `executor/copilotcli.Executor.Execute` addresses this two ways: (1) it never passes `--allow-tool`/`--allow-all-tools`, so the CLI is invoked with no tool grants at all; (2) it runs `git status --porcelain` in the workspace immediately before and after the call and compares the two outputs byte-for-byte, refusing with a named, specific error if anything differs — a defensive check no other Executor in this repo needs, since none of the others carry this particular risk.

  Mirrors `executor/geminicli`'s structure closely (`runner` interface, `execRunner`, prompt piped via stdin to avoid `ARG_MAX` risk with a large gathered-context prompt, the same `executionError` shape) but parses raw text via `executor.ParsePatch` directly rather than JSON-decoding a `{response, error}` envelope, since the Copilot CLI's own docs don't document a structured output mode the way the Gemini CLI's `--output-format json` does. `cmd/foundry/main.go`'s `namedExecutor` gained a `"copilot"` case. New tests (`executor/copilotcli/copilotcli_test.go`, using a real temporary git repository — the `git status --porcelain` check shells out directly, unlike the mockable `runner` seam the CLI invocation itself goes through): success with args/stdin captured, `TestExecute_NeverGrantsToolAccess` (a regression guard scanning every argument for `allow-tool`/`allow-all`), model flag appended only when set, `TestExecute_DetectsDirectWorkspaceMutation` (the one test unique to this package — a fake runner that writes a file to simulate the CLI editing the workspace directly, confirming Execute refuses rather than silently succeeding), missing executable, timeout, failure with/without stderr content, empty output, no-diff-in-output, no workspace configured. Documented in [getting-started.md](../04-guides/getting-started.md), naming the safety mechanism and the live-validation gap explicitly rather than presenting it as equally proven as `gemini`/`gemini-api`. **Not validated live**: no `copilot` CLI installed in this environment — genuinely the highest-risk Executor in the repo to trust unattended, precisely because the safety mechanism protecting it has itself only been exercised against a fake runner, never a real agentic CLI's actual behavior. `go build ./...`, `go vet ./...`, `go test -race ./...` all green; `gofmt -l .` clean.
- **2026-07-23** — Added AI code review as a supplementary, non-deterministic `engine.Verifier`, answering the maintainer's own follow-up question ("can code review be done by a model other than Copilot, feeding into fixes?" — answered "yes" first, then implemented on explicit approval) without any new Pipeline Step kind. New `verify.Compose` (`verify/compose.go`) is a general `engine.Verifier` combinator — any non-"pass" verdict fails the whole composition, findings concatenate — mirroring `gatherer.Compose`'s existing pattern exactly rather than inventing a new composition shape. New `verify/aireview` package implements `engine.Verifier` against any OpenAI-Chat-Completions-compatible endpoint (reusing `executor/openai`'s already-established request/response shapes), asking a model to review a proposed unified diff and respond with a parseable `VERDICT: pass|fail` plus findings; any response it can't parse into a clear verdict is always treated as `fail`, never silently passed through, with the raw response text preserved in the findings for debugging. Wired at both composition roots (`session.NewSession`, `cmd/foundry/commands/do.go`'s `wireEngine`) as `verify.Compose(gate, aireview.NewVerifier(...))` — the deterministic Gate always runs first and is never replaced, per [trust.md](../02-architecture/trust.md)'s stated preference for deterministic checks before model judgment. `project.Config` gained three opt-in fields (`ai_review_model`, `ai_review_base_url`, `ai_review_api_key_env`); a model set without a base URL is a clear, named configuration error at construction time, not a silently-inert verifier — there is no single default endpoint across vendors (OpenAI, Gemini's API, Ollama, Groq, DeepSeek) to fall back to. No new engine mechanism was needed for its findings to become fix context: a failed verify Step's findings already flow into the next generate attempt's considered Context via the existing repair mechanism (`engine/strategy.go`'s `repairContext`), the same path any deterministic validator failure already uses. New tests: `verify/compose_test.go` (no-verifiers passes, identical single verifier, all-pass yields pass, any failure fails the whole composition, an error stops composition), `verify/aireview/aireview_test.go` (pass-no-findings, pass-with-findings-doesn't-fail, fail, malformed-response-is-fail, transport error, unauthorized, case-insensitive verdict label, empty-content-is-fail), `session.TestNewSession_AIReviewModelRequiresBaseURL`, `commands.TestWireEngine_AIReviewModelRequiresBaseURL`, `project.TestLoadConfig_DecodesAIReviewFields`. Documented in [getting-started.md](../04-guides/getting-started.md)'s new "AI code review" section. **Not validated live**: no real OpenAI/Gemini/Ollama/Groq/DeepSeek endpoint or key available in this environment — fixture-tested against the documented Chat Completions API shape only. `go build ./...`, `go vet ./...`, `go test -race ./...` all green; `gofmt -l .` clean.

- **2026-07-23** — Wired real per-Step model diversification into `/issue`, closing a real gap between what was asked ("diversificando modelos" across the fetch → analyze → implement → verify → PR cycle) and what shipped: `.foundry/pipelines/issue.json`'s `plan` Step now pins `"executor": "planner"`, and a new `.foundry/executors.json` maps `"planner"` to the Gemini CLI vendor (`{"vendor": "gemini", "model": "gemini-3.5-flash"}`) — so the initial ticket analysis runs on a different, cheaper model than `implement`'s (the default Executor, left unpinned). No code change was needed: `engine.Router.Resolve` and `project.BuildExecutorRegistry` already supported per-Step Executor pins since ADR-0005/ADR-0006; this is this repository's own dogfooded Pipeline document actually exercising the `executor` field for the first time. Fixed two stale doc claims caught in the process: [pipelines.md](../04-guides/pipelines.md)'s schema section previously asserted `executor` and `feeds_forward` were "unused by any document above" — true for `feeds_forward` still, no longer true for `executor`; and its "What's shipped" section undercounted the project-level Pipelines (three named, four actually present — `issue.json` had never been added to that list). Documented in [getting-started.md](../04-guides/getting-started.md)'s `/issue` section. No new tests: this is Pipeline-document and `executors.json` configuration only, already covered by `engine/router_test.go`'s existing `Resolve`/pin-not-registered tests. `go build ./...`, `go vet ./...`, `go test -race ./...` all green; `gofmt -l .` clean.

- **2026-07-23** — Gave this repository its own real `.foundry/config.json` and fixed a stale doc line, both per the maintainer's own request to "generar la base aquí en este proyecto del mismo uso de la app" (build the actual usage base in this project itself). Until now every `/issue`/AI-review run had only ever been tried against throwaway test repositories — this repository's own checkout never had a config file at all. New `.foundry/config.json`: `ticket_provider: "github"` (works immediately, reusing the already-authenticated `gh` CLI session, the same path validated live earlier); `require_approval_before_remote_publish: true` plus `remote_publish_token_env: "GH_TOKEN"` (requires exporting a real GitHub PAT before `/issue`'s `remote-pr` apply Step can actually push/open a PR — `gh auth login`'s cached session alone is not read by `vcs.GitHubPRApplier`, unlike the ticket-fetch path); `ai_review_model: "llama3"` / `ai_review_base_url: "http://localhost:11434/v1/chat/completions"` (points at a local Ollama instance, chosen per the maintainer's own explicit choice — free, no key required — over OpenAI/Groq; **Ollama is not installed in this environment**, so this is configuration-only, not a working reviewer yet, honestly documented as such rather than presented as validated). Documented in [getting-started.md](../04-guides/getting-started.md)'s new "This repository's own base configuration" section, naming each field's real remaining prerequisite. Also fixed [roadmap.md](roadmap.md)'s stale "Parallel track: Ticket-driven Acts" line, which still read "GitHub today, Jira planned next" despite all four providers having shipped days earlier and the very next line already saying so — the same file contradicting itself, the class of drift [AGENTS.md](../../AGENTS.md) calls out as a defect to fix, not leave. No code changes; `go build ./...`, `go vet ./...`, `go test -race ./...` all green; `gofmt -l .` clean.
