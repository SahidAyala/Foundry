# Implementation Status

> **A single, living dashboard of what's actually built vs. still planned — across the roadmap, every RFC, every ADR, and every architecture document — so a new implementation session can start here instead of reconstructing it from `git log` or re-reading five RFCs.** This is a status *index*, not a source of truth: each row links to the document that actually defines the thing. Update the relevant row (and add a dated line to the changelog at the bottom) whenever a milestone ships, an RFC's scope changes, or an ADR is accepted — the failure mode this file exists to prevent is exactly what happened to the old `M0-IMPLEMENTATION-BACKLOG.md` checklist (see [archive/obsolete/M0-IMPLEMENTATION-BACKLOG.md](../archive/obsolete/M0-IMPLEMENTATION-BACKLOG.md)): left unread while the code moved on, until it actively misled.
>
> **Last full audit: 2026-07-20.** Two different axes are tracked separately per item, because they move independently in this project: **ratification** (has a human formally accepted this decision under [ADR-0000](../03-adrs/ADR-0000-governance-and-ratification-process.md)?) and **implementation** (does the code actually do this today?). A document can be fully implemented and still formally unratified — that is this project's normal state, not a defect, per [m0-plan.md](../04-guides/m0-plan.md)'s "nothing blocks writing code."

## 1. Roadmap milestones

The full milestone *plan* (themes, "usable system" definitions) lives in [roadmap.md](roadmap.md). Current shipped status:

| Milestone | Status | Notes |
|---|---|---|
| **M0** — Walking skeleton | Shipped, validated live | All of M0.0–M0.3 (`foundry do`, scripted + real Executor, budget, repair, `log`/`show`). Historical plan: [archive/obsolete/M0-IMPLEMENTATION-BACKLOG.md](../archive/obsolete/M0-IMPLEMENTATION-BACKLOG.md). Confirmed working end-to-end with a live Executor on 2026-07-18 — see §7. |
| **M1** — Deterministic core | Shipped | Verified/judged/budgeted Acts, immutable filesystem Record, replay (`foundry replay`) and resume (`foundry resume`, mid-Pipeline checkpoints). |
| **M2** — Reusable production + mock executor | Shipped | Authored Pipeline documents (`.foundry/pipelines/*.json`) are the reusable-Act artifact, per [ADR-0004](../03-adrs/ADR-0004-reusable-act-template-format-and-evolution-policy.md) (ratified and implemented 2026-07-20); `executor.ScriptedExecutor` is the fixture Executor; bounded repair works. |
| **M3** — Real executors | Shipped, partially | Two real Executors behind a `Router` — Claude Code (default) and OpenAI (named). Routing is **explicit-pin-only**; capability-based negotiation is deferred (see RFC-0002 row below). **Actual-cost reporting shipped 2026-07-21** ([ADR-0011](../03-adrs/ADR-0011-cost-as-a-first-class-constraint.md)): `executor/openai.Executor` now decodes the Chat Completions response's `usage` object (previously received and discarded) into `domain.Outcome.ActualCostUSD`; `executor/claude.ClaudeExecutor` still reports none (its subprocess has no billing signal — a structural limit, not a gap). |
| **M4** — Knowledge & evidence | Partial | Authored Knowledge write + naive lexical retrieval shipped. [ADR-0007](../03-adrs/ADR-0007-knowledge-and-semantic-store.md) (ratified 2026-07-21) closes the store's policy questions — note format, `.foundry/knowledge/` durability, format stability — but Derived Knowledge, semantic retrieval, and provenance scoring remain explicitly declined until each has a named trigger, so this milestone stays code-partial. |
| **M5** — Integration & visibility | Shipped, partially | VCS/PR publish shipped. **Worktree isolation for concurrent Acts shipped 2026-07-20**: `workspace.Workspace` now realizes its throwaway branch inside a separate `git worktree`, never switching the developer's own checked-out branch — two Acts against the same repo no longer race (`workspace/workspace_test.go`'s `TestWorkspace_TwoWorkspaces_SameRepo_NoInterference`). **Change-review UI: a first real slice shipped 2026-07-21** — `foundry show` now colors its patch/verdict exactly like the live approval prompt, and renders a per-Step trace (`domain.Act.Steps`: each declared Step's own verdict and duration) that was already recorded but not previously surfaced, closing a real gap for multi-verify Pipelines like the built-in `review` Pipeline (`cli/cli.go`'s `formatAct`, `cli/render.go`'s `renderStepVerdictLabel`). **Observability: a first slice shipped 2026-07-21** — an opt-in (`FOUNDRY_LOG` env var) structured JSON event log to stderr via `engine.SlogReporter`, fanned out alongside the existing human `ProgressReporter` via a new `engine.MultiReporter`, wired at both composition roots (`cmd/foundry/commands/do.go`, `session/run_pipeline_command.go`) through one shared `cli.NewReporter` helper. Default behavior (env var unset) is byte-for-byte unchanged. Every milestone theme now has at least a first shipped slice. **Paging for long diffs/findings shipped 2026-07-21**: `foundry show` and the live approval prompt now pipe content longer than 40 lines through `$PAGER` (falling back to `less -R`, preserving color) when output is an interactive terminal — `cli/pager.go`'s `maybePage`, gated by the same explicit `page bool` pattern `renderDiff`/`renderVerdict` already established rather than re-deriving terminal-ness internally; a pager failure (not installed, broken `$PAGER`) always falls back to printing directly, never losing output. **Actual-cost reporting shipped 2026-07-21** ([ADR-0011](../03-adrs/ADR-0011-cost-as-a-first-class-constraint.md)): `foundry show` renders an "Actual cost" line (with honest "N of M generate Steps" coverage) whenever at least one generate Step's Executor reported a real, post-execution cost — omitted entirely otherwise, never a permanent placeholder; `engine.SlogReporter`'s new `act.execute.done` event carries the same signal into the `FOUNDRY_LOG` stream. |
| **M6** — Extensibility | Not started | No third-party extension surface exists. **[ADR-0008](../03-adrs/ADR-0008-extension-isolation-and-contract-versioning.md) (ratified 2026-07-21) resolves the milestone's one open policy question** (isolation mechanism, contract versioning) as a deliberate non-decision — no mechanism chosen, deferred to a real third-party extension request — rather than leaving it ambiguous; it also corrects [ADR-0001](../03-adrs/ADR-0001-language-and-toolchain.md)'s stale gRPC/protobuf pre-commitment. The milestone itself remains code-not-started; this ADR closes a documentation gap, not the milestone. |
| **M7** — Stability | Not started | No contract freeze; several open decisions below still gate it. |

Getting-started instructions for what's usable today live in [../04-guides/getting-started.md](../04-guides/getting-started.md).

## 2. RFCs — decision status vs. code status

All five RFCs remain **Draft — Proposed**: a governance process exists ([ADR-0000](../03-adrs/ADR-0000-governance-and-ratification-process.md)), but none has been individually ratified through it yet. Their *implementation* status varies widely and is tracked here so "unratified" is never misread as "unbuilt."

| RFC | Ratified? | Implementation | Pending |
|---|---|---|---|
| [RFC-0001](../01-rfcs/RFC-0001-vision-and-product-philosophy.md) — Vision & product philosophy | No | N/A — a values/philosophy document, not a code deliverable; already reflected throughout the architecture docs and the system's actual behavior (trust gate, human approval, deterministic-first). | Formal ratification only. |
| [RFC-0002](../01-rfcs/RFC-0002-pipeline-execution-runtime.md) — Pipeline execution runtime | No | **~85%.** Phases 1–6 and 8 shipped: Step trace on `Act`, `Strategy`/`PipelineStrategy`, Approve/Record as declared Step kinds, `FixedStrategy` fully retired, Router (explicit-pin), interactive shell. **Both of Phase 0's prerequisite ADRs now accepted and implemented:** [ADR-0005](../03-adrs/ADR-0005-executor-contract-and-capability-model.md) (Executor contract) and [ADR-0004](../03-adrs/ADR-0004-reusable-act-template-format-and-evolution-policy.md) (Reusable-Act template format, ratified and its Decision 4 shipped 2026-07-20 — §3 below). **§7's routing-policy gap is now closed**: [ADR-0006](../03-adrs/ADR-0006-routing-and-policy.md) (drafted and ratified 2026-07-20 — §3 below) ratifies today's explicit-pin Router and reaffirms that negotiation/failover stay deferred. | **Phase 7** — capability-based negotiation/failover (cost/latency/quality weighting) — deliberately deferred by [ADR-0006](../03-adrs/ADR-0006-routing-and-policy.md) itself, no real multi-Executor-per-Capability case yet. |
| [RFC-0003](../01-rfcs/RFC-0003-interactive-assistant-and-multi-executor-pipelines.md) — Interactive assistant & multi-executor pipelines | No | **~90%.** Interactive session & slash commands (§3.1), project-local Pipeline authoring via `/init` (§3.2), VCS/PR automation (§4.1, [ADR-0010](../03-adrs/ADR-0010-vcs-pr-integration-and-apply-targets.md)) all shipped. **§6's "CLI & output contract" gap is now closed**: [ADR-0009](../03-adrs/ADR-0009-cli-and-output-contract.md) (drafted and ratified 2026-07-20 — §3 below) formally ratifies the interactive-primary product-shape direction the maintainer confirmed informally on 2026-07-18, and all its Decisions, including 6–8, are implemented the same day: `/help` is a real command (`session.HelpCommand`), `engine.PipelineProvider`/`BuiltinProvider`/`project.FilesystemPipelineProvider` are renamed to `PipelineSource`/`BuiltinPipelineSource`/`FilesystemPipelineSource`, and Session/Slash Command are documented in [terminology.md](../05-reference/terminology.md). | Same gap as RFC-0002: real capability-based routing validated against a multi-role lifecycle (§3.3) is not built — today's Router is explicit-pin, two vendors. |
| [RFC-0004](../01-rfcs/RFC-0004-multi-executor-router-and-publish-policy.md) — Multi-executor router & publish policy | No | **100% of its own 6 Pieces shipped** (Capability/Router/ExecutorRegistry, [ADR-0005](../03-adrs/ADR-0005-executor-contract-and-capability-model.md) accepted, second Executor OpenAI, Knowledge-lite capture, per-Step budget accounting, VCS/PR via [ADR-0010](../03-adrs/ADR-0010-vcs-pr-integration-and-apply-targets.md) accepted). §4's "Routing & policy" backlog ADR is now drafted and ratified as [ADR-0006](../03-adrs/ADR-0006-routing-and-policy.md). | Nothing code-side; only the RFC document's own ratification. |
| [RFC-0005](../01-rfcs/RFC-0005-authored-knowledge-retrieval.md) — Authored Knowledge retrieval | No | **100% of its own 4 pieces shipped** (formalized `.foundry/knowledge/` as the Authored store, `gatherer.Compose`, naive lexical Knowledge Gatherer, wired into both composition roots). | Nothing code-side for what this RFC proposed. Explicitly out of its scope, and not yet proposed by any RFC: Derived Knowledge indexing, semantic retrieval, Authored-knowledge format stability. |

## 3. ADRs

Accepted vs. backlog is tracked canonically in [../03-adrs/README.md](../03-adrs/README.md) — this section only flags what's implementation-relevant beyond that table:

- **Accepted (12):** [ADR-0000](../03-adrs/ADR-0000-governance-and-ratification-process.md) (governance), [ADR-0001](../03-adrs/ADR-0001-language-and-toolchain.md) (Go — both pending amendments now resolved, see ADR-0002 and ADR-0008 below), [ADR-0002](../03-adrs/ADR-0002-persistence-content-addressing-and-on-disk-layout.md) (Persistence, content-addressing & on-disk layout — ratified 2026-07-20, no code changes needed), [ADR-0003](../03-adrs/ADR-0003-replay-and-determinism-contract.md) (Replay & determinism contract — ratified 2026-07-20, no code changes needed), [ADR-0004](../03-adrs/ADR-0004-reusable-act-template-format-and-evolution-policy.md) (Reusable-Act template format — ratified and implemented 2026-07-20), [ADR-0005](../03-adrs/ADR-0005-executor-contract-and-capability-model.md) (Executor contract — implemented), [ADR-0010](../03-adrs/ADR-0010-vcs-pr-integration-and-apply-targets.md) (VCS/PR — implemented), [ADR-0009](../03-adrs/ADR-0009-cli-and-output-contract.md) (CLI & output contract — ratified and implemented 2026-07-20), [ADR-0006](../03-adrs/ADR-0006-routing-and-policy.md) (Routing & policy — ratified 2026-07-20, no code changes needed), [ADR-0007](../03-adrs/ADR-0007-knowledge-and-semantic-store.md) (Knowledge & semantic store — ratified 2026-07-21, no code changes needed), [ADR-0011](../03-adrs/ADR-0011-cost-as-a-first-class-constraint.md) (Cost as a first-class constraint — ratified 2026-07-21, follow-up implementation PR shipped same day), [ADR-0008](../03-adrs/ADR-0008-extension-isolation-and-contract-versioning.md) (Extension isolation & contract versioning — ratified 2026-07-21, no code changes needed). **The ADR backlog is now empty** — every row identified so far is Accepted; new rows are added only as new architectural decisions are genuinely identified as needed. ADR-0008 decides no isolation mechanism and no versioning policy (both genuinely undecided — no third-party extension exists to design against); its real work corrects [ADR-0001](../03-adrs/ADR-0001-language-and-toolchain.md) clause 4 (and downstream Consequences/"Harder"/Future-ADR-Dependencies text), which had asserted gRPC/protobuf as a **binding** decision even after [extensibility.md](../02-architecture/extensibility.md) and [OQ-005](../06-open-questions/OQ-005-extension-isolation.md) had already withdrawn that assertion as premature — the same class of live contradiction [ADR-0002](../03-adrs/ADR-0002-persistence-content-addressing-and-on-disk-layout.md) corrected for ADR-0001's SQLite clause. R2 (language-agnostic extension boundary) remains fully binding; the concrete mechanism is deferred to a real third-party extension request. ADR-0011 closes [ADR-0005](../03-adrs/ADR-0005-executor-contract-and-capability-model.md)'s Open Question 1 with a firm "no" (`CostEstimator` stays optional — Claude Code's subprocess structurally has no billing signal); proposes optional, additive `ActualCostUSD` on `Outcome`/`StepRecord`/`Act` as reported Evidence only, never a second Budget gate; declines to make Budget's hardcoded ceilings configurable or to build cost reconciliation until each has a named trigger. ADR-0009 ratifies the interactive-primary product shape, a pre-1.0 "change freely, but never silently" policy for the flag CLI and slash commands, the existing exit-code convention, and declines a JSON output mode for now; fixes the phantom `/help` banner reference and renames `PipelineProvider`→`PipelineSource` (RFC-0003 §5's flagged terminology tension) — see the RFC-0003 row above and the ADR's own Review Checklist. ADR-0006 ratifies the already-shipped explicit-pin `Router`/`ExecutorRegistry` and the Router-reserved Step fields' behavior; reaffirms [ADR-0005](../03-adrs/ADR-0005-executor-contract-and-capability-model.md) Decision 4 ("capability declared, not verified"); explicitly declines to build capability-based negotiation or failover before their own named trigger fires; fixes README.md's live "Provider independence" defect — see the RFC-0002/RFC-0004 rows above and the ADR's own Review Checklist. ADR-0002 ratifies `record.FileStore`/`record.CheckpointStore` exactly as already shipped, closes [OQ-008](../06-open-questions/OQ-008-in-progress-act-persistence.md), gives [I8](../05-reference/invariants.md) a concrete consequence (commit `.foundry/acts/` to a project's own repository), and **corrects a real contradiction**: [ADR-0001](../03-adrs/ADR-0001-language-and-toolchain.md) had pre-committed it to an embedded pure-Go SQLite storage driver — a design that was never built and directly conflicted with [principles.md](principles.md)'s higher-precedence Filesystem-first mandate; ADR-0001's two affected clauses are now corrected in place. ADR-0003 ratifies `replay.Verify` exactly as already shipped (same-version-only; real Validators/Gate re-executed against the recorded Patch, never the Executor; a verdict divergence surfaced as honest data, never hidden); closes [OQ-003](../06-open-questions/OQ-003-replay-across-versions.md) and [OQ-004](../06-open-questions/OQ-004-validator-determinism.md) together, **explicitly superseding OQ-004's own recommendation** (record-and-replay validator findings, never re-run the tool) with the stronger design actually built. ADR-0007 ratifies `workspace.KnowledgeNoteApplier`/`workspace.ProjectDocApplier`/`knowledge.Gatherer` exactly as already shipped (unstructured Markdown prose, no front-matter); closes the Authored-Knowledge format-stability question by observing unparsed text has nothing to destabilize; extends ADR-0002's `.foundry/acts/` commit-to-git convention to `.foundry/knowledge/`; and **explicitly declines** Derived-Knowledge indexing, semantic retrieval, provenance scoring, and note curation until each has a named, concrete trigger, rather than leaving them ambiguous.
- **Drafted, pending ratification (0):** none currently.
- **Backlog, implementation-relevant:** capability-based negotiation and failover (RFC-0002 §7 layers 2–3) remain unbuilt regardless of [ADR-0006](../03-adrs/ADR-0006-routing-and-policy.md)'s ratification status — that ADR explicitly defers both until their own named trigger (≥2 real Executors competing for one Capability in production) fires, not until the ADR itself is accepted.

## 4. Architecture documents (`docs/02-architecture/`)

All PROVISIONAL (nothing in the repository is CANONICAL yet — [ADR-0000](../03-adrs/ADR-0000-governance-and-ratification-process.md) made the ceiling reachable, but each document still needs its own ratification). Maturity nuance for each is tracked in [../05-reference/concepts.md](../05-reference/concepts.md)'s maturity index — this table adds *implementation* coverage, which that index doesn't track:

| Document | Answers | Implementation coverage |
|---|---|---|
| [domain.md](../02-architecture/domain.md) | What are the domain concepts? | High — Act, Intent, Evidence, Outcome, Judgment, Authority, Record all exist as real types (`domain/act.go`) and are exercised end-to-end. |
| [execution.md](../02-architecture/execution.md) | How does the system produce outcomes? | High — Pipeline-as-Strategy, Steps, repair are all real (`engine/strategy.go`, `engine/document.go`). |
| [trust.md](../02-architecture/trust.md) | How is trust established? | High for the mechanics (Verify/Gate/Judgment/Approve all run today); the *verification guarantee's* exact strength remains [OQ-004](../06-open-questions/OQ-004-validator-determinism.md), unresolved by design. |
| [knowledge.md](../02-architecture/knowledge.md) | What is durable knowledge? | Partial — Authored Knowledge (write + naive retrieval) is real; Derived Knowledge is pure concept, no code. |
| [system-context.md](../02-architecture/system-context.md) | What are the system boundaries? | Partial — today's actual (local, single-user) boundary is accurately described; the "remote is additive" and concurrency claims are unproven, no code exercises them. |
| [extensibility.md](../02-architecture/extensibility.md) | What may be extended? | Low — requirements only. No third-party extension surface exists (M6 not started); the isolation mechanism is deliberately undecided, ratified as such by [ADR-0008](../03-adrs/ADR-0008-extension-isolation-and-contract-versioning.md) (closes [OQ-005](../06-open-questions/OQ-005-extension-isolation.md)). |

## 5. Guides (`docs/04-guides/`)

| Guide | Status | Use it for |
|---|---|---|
| [getting-started.md](../04-guides/getting-started.md) | Active | Installing and running Foundry today — the actual first stop for a new user. |
| [pipelines.md](../04-guides/pipelines.md) | Active | Authoring/reading a Pipeline document. |
| [development.md](../04-guides/development.md) | Active | Durable build/test/CI approach (not milestone-specific). |
| [m0-plan.md](../04-guides/m0-plan.md) | Historical (kept as design rationale) | *Why* M0 was scoped the way it was — not a current plan. |
| [multi-executor-router-implementation-plan.md](../04-guides/multi-executor-router-implementation-plan.md) | Historical (all 6 Pieces shipped) | Reference for *how* RFC-0004 was actually sequenced. |
| [interactive-session-implementation-plan.md](../04-guides/interactive-session-implementation-plan.md) | Check before reuse | Sequencing plan for RFC-0003's interactive session — verify against §2 above before treating any step as still-pending. |
| [contributing.md](../04-guides/contributing.md), [documentation.md](../04-guides/documentation.md), [release.md](../04-guides/release.md) | Active | Process guides, not milestone-specific — not audited for staleness here. |

## 6. Reference & open questions — pointers only

- Concept-by-concept maturity: [../05-reference/concepts.md](../05-reference/concepts.md) (do not duplicate here — that file is the canonical maturity index).
- Canonical vocabulary: [../05-reference/terminology.md](../05-reference/terminology.md).
- Invariants: [../05-reference/invariants.md](../05-reference/invariants.md).
- Open architectural questions (non-canonical, active deliberation): [../06-open-questions/README.md](../06-open-questions/README.md).

## 7. Real-world validation log

Distinct from the audits above (which read code and docs): this tracks actual runs of the built binary against a real repository with a live Executor — the strongest evidence available that the walking skeleton still works end-to-end, not just that its unit tests pass in isolation.

- **2026-07-18** — Ran a full interactive session (`/init` → `/bug "<intent>"` → real Claude Code Executor → `go build`/`go test` verification → human approval → apply → record) against a throwaway Go-module sample repository. Confirmed working end-to-end: real patch generation, verification, approval, Act recording, and both `foundry log`/`foundry show` history inspection all matched their documented behavior. Surfaced one real bug in the process — the `go-build` validator (`verify/detect.go`) left a compiled binary artifact in the repository on every Act against a `package main` module — fixed same day (`fix(verify): Stop go-build validator from leaking a binary into the repo`).

## Changelog

- **2026-07-18** — Initial audit. Archived the stale M0 checklist docs; added this file; registered the confirmed interactive-primary product-shape direction against ADR-0009 in [../03-adrs/README.md](../03-adrs/README.md).
- **2026-07-18** — First real dogfooding run (§7): validated the walking skeleton end-to-end with a live Claude Code Executor; found and fixed a `go-build` validator bug that leaked a compiled binary into the repository on every Act.
- **2026-07-20** — Drafted and ratified [ADR-0004](../03-adrs/ADR-0004-reusable-act-template-format-and-evolution-policy.md) (Reusable-Act template format & evolution policy) under [ADR-0000](../03-adrs/ADR-0000-governance-and-ratification-process.md), satisfying RFC-0002 Phase 0's second prerequisite ADR.
- **2026-07-20** — Implemented ADR-0004 Decision 4: `engine.DecodePipelineDocument` now rejects unknown fields with a descriptive error naming the field, the enclosing Step (or index), and pointing at [pipelines.md](../04-guides/pipelines.md); documented in that guide's new "Field evolution and unknown fields" section.
- **2026-07-20** — Second full audit. Re-verified every roadmap milestone, RFC, and ADR row against the actual codebase (`engine/router.go`, `engine/executor_registry.go`, `knowledge/`, `vcs/`, absence of any worktree-isolation or extension-surface code) and against [roadmap.md](roadmap.md) — no drift found beyond the ADR-0004 rows already updated above.
- **2026-07-20** — Drafted [ADR-0009](../03-adrs/ADR-0009-cli-and-output-contract.md) (CLI & output contract), closing RFC-0003 §6's named gap. Status: Proposed, not yet ratified.
- **2026-07-20** — Implemented ADR-0009 Decisions 6–8: `session.HelpCommand` backs `/help` (listing every registered slash command via a new `CommandRegistry.List`); `engine.PipelineProvider`/`BuiltinProvider` and `project.FilesystemPipelineProvider` are renamed to `PipelineSource`/`BuiltinPipelineSource`/`FilesystemPipelineSource` throughout the codebase and [pipelines.md](../04-guides/pipelines.md); **Session** and **Slash Command** added to [terminology.md](../05-reference/terminology.md) and [concepts.md](../05-reference/concepts.md). Decisions 2 and 4 (pre-1.0 change policy, exit-code convention) documented in [release.md](../04-guides/release.md)'s new "CLI & output contract" section.
- **2026-07-20** — Ratified [ADR-0009](../03-adrs/ADR-0009-cli-and-output-contract.md) (CLI & output contract) under [ADR-0000](../03-adrs/ADR-0000-governance-and-ratification-process.md), the same day it was drafted and implemented. Status row, [README.md](../03-adrs/README.md)'s backlog table, and this file all updated in the same ratifying pass; `release.md`'s "intended, not ratified" disclaimer updated to reflect the CLI & output contract surface is no longer blocked.
- **2026-07-20** — Drafted and ratified [ADR-0006](../03-adrs/ADR-0006-routing-and-policy.md) (Routing & policy) under [ADR-0000](../03-adrs/ADR-0000-governance-and-ratification-process.md), the same day, closing RFC-0002 §7 / RFC-0004 §2.1–§2.3's named routing-policy gap. Ratifies the already-shipped explicit-pin `Router`; formally re-examines and reaffirms [ADR-0005](../03-adrs/ADR-0005-executor-contract-and-capability-model.md) Decision 4 per that ADR's own named requirement to do so; closes [ADR-0004](../03-adrs/ADR-0004-reusable-act-template-format-and-evolution-policy.md) Decision 7's deferral on the Router-reserved Step fields' behavior; declines to build capability-based negotiation or failover before their own named trigger fires. Also fixes a live use of the retired term "Provider" in [README.md](../03-adrs/README.md)'s backlog table ("Provider independence" → "Vendor independence"). No code changes required — the ADR ratifies already-shipped, already-tested behavior.
- **2026-07-20** — Shipped M5's "Worktree isolation for concurrent Acts": `workspace.Workspace` (`workspace/workspace.go`) now creates its throwaway branch inside a separate `git worktree` — mirroring `StagedVerifier`'s already-proven pattern (`workspace/staged.go`) — instead of `git checkout -b` directly in the developer's repository. The developer's own checked-out branch is never switched for any part of an Act's lifetime; only `Land`'s single, final `git apply` ever touches it. Fixes two latent bugs as a side effect: (1) `Clean` no longer runs `git reset --hard`/`git clean -fd` against the developer's real directory, so any of the developer's own pre-existing uncommitted changes now survive a discarded Act; (2) `Land` now checks that the repository's branch hasn't changed since the Act started, refusing (rather than silently misapplying) if the developer switched branches mid-Act — on any failure, the worktree/branch/patch file are preserved for manual recovery, never destroyed. `workspace/staged.go` also got a one-line fix for the same root cause (its worktree directory's leaf name could collide with a concurrent verification's under git's own internal `.git/worktrees/<name>` naming). No public API changes — `ApplyAct`, `GitApplier`, and `vcs.GitHubPRApplier` are all untouched. Verified with `go test -race ./...` (new test: `TestWorkspace_TwoWorkspaces_SameRepo_NoInterference`) and a manual smoke run. A gap was surfaced during this work (see next entry).
- **2026-07-20** — Fixed the gap surfaced above: `cli.go`'s `Do`/`finalize` never recorded an approved Act if `workspace.ApplyAct` failed after approval (a tension with I8, immutable history) — investigation confirmed this only affects the CLI's own direct-apply fallback (a Pipeline declaring no apply Step, e.g. the built-in `default`/`review` Pipelines): a Pipeline that declares its own `apply`/`record` Steps already recovers correctly via `foundry resume` (its checkpoint, saved after the preceding `approve` Step, lets `engine.Resume` retry the failed `apply` Step exactly), but `engine.Resume` refuses outright once a Pipeline has no more declared Steps left to run, which is always true here by the time this fallback path executes. Fix: on an apply failure, `finalize` now sets a new `engine.VerdictApplyFailed` verdict, appends the error as a `CheckedFindings` entry, and records the Act — so the approval decision and patch survive even though the apply itself did not land — before returning the error. New test: `cli.TestCLI_Do_ApprovedButApplyFailsIsStillRecorded`.
- **2026-07-20** — Drafted [ADR-0002](../03-adrs/ADR-0002-persistence-content-addressing-and-on-disk-layout.md) (Persistence, content-addressing & on-disk layout), the first ADR in the backlog's dependency order. Ratifies `record.FileStore`/`record.CheckpointStore` exactly as shipped, closes [OQ-008](../06-open-questions/OQ-008-in-progress-act-persistence.md), and gives I8 a concrete practical consequence. Surfaced and corrects a real contradiction: [ADR-0001](../03-adrs/ADR-0001-language-and-toolchain.md)'s pre-commitment to an embedded pure-Go SQLite storage driver was never built and conflicts with [principles.md](principles.md)'s higher-precedence Filesystem-first mandate. Status: Proposed, not yet ratified.
- **2026-07-20** — Ratified [ADR-0002](../03-adrs/ADR-0002-persistence-content-addressing-and-on-disk-layout.md) under [ADR-0000](../03-adrs/ADR-0000-governance-and-ratification-process.md), the same day it was drafted. [ADR-0001](../03-adrs/ADR-0001-language-and-toolchain.md)'s two stale persistence clauses (its Decision section and its Consequences section) are corrected in place, pointing at ADR-0002 rather than describing an embedded SQLite driver that was never built; [OQ-008](../06-open-questions/OQ-008-in-progress-act-persistence.md) marked RESOLVED (and its own index row in [open-questions/README.md](../06-open-questions/README.md)); [roadmap.md](roadmap.md) open decision 6 marked RESOLVED; [invariants.md](../05-reference/invariants.md)'s closing note no longer lists I8 as pending; [release.md](../04-guides/release.md) gets a new "Record format & persistence" section and an updated disclaimer; [getting-started.md](../04-guides/getting-started.md) gains a commit-`.foundry/acts/`-to-git recommendation; [concepts.md](../05-reference/concepts.md) and [README.md](../03-adrs/README.md)'s backlog table updated to match.
- **2026-07-20** — Corrected a leftover drift from ADR-0002's ratification: [trust.md](../02-architecture/trust.md)'s own "Unresolved: the durability classification of the Record" callout was not updated in that pass — fixed now to cite ADR-0002.
- **2026-07-20** — Drafted [ADR-0003](../03-adrs/ADR-0003-replay-and-determinism-contract.md) (Replay & determinism contract), the second ADR in the backlog's dependency order. Ratifies `replay.Verify` exactly as shipped and closes OQ-003/OQ-004 together, explicitly superseding OQ-004's own never-built "record-and-replay validator findings" recommendation with the stronger, actually-shipped design (real Validators/Gate re-executed, divergence surfaced honestly). Status: Proposed, not yet ratified.
- **2026-07-20** — Ratified [ADR-0003](../03-adrs/ADR-0003-replay-and-determinism-contract.md) under [ADR-0000](../03-adrs/ADR-0000-governance-and-ratification-process.md), the same day it was drafted. [execution.md](../02-architecture/execution.md)'s "Determinism and replay" section corrected (cites this ADR and ADR-0002 by name, drops the stale "Unresolved" callout); [trust.md](../02-architecture/trust.md)'s remaining "Unresolved: validator determinism" callout resolved; [OQ-003](../06-open-questions/OQ-003-replay-across-versions.md) and [OQ-004](../06-open-questions/OQ-004-validator-determinism.md) marked RESOLVED (both their own files and [open-questions/README.md](../06-open-questions/README.md)'s index); [roadmap.md](roadmap.md) open decisions 4–5 marked RESOLVED; [invariants.md](../05-reference/invariants.md)'s closing note no longer lists I3/I6 as pending; [concepts.md](../05-reference/concepts.md) and [README.md](../03-adrs/README.md)'s backlog table updated to match. Only "Knowledge & semantic store," "Extension isolation & contract versioning," and "Cost as a first-class constraint" remain in the ADR backlog.
- **2026-07-21** — Drafted [ADR-0007](../03-adrs/ADR-0007-knowledge-and-semantic-store.md) (Knowledge & semantic store), the next ADR in the backlog's dependency order. Ratifies `workspace.KnowledgeNoteApplier`/`workspace.ProjectDocApplier`/`knowledge.Gatherer` exactly as shipped: unstructured Markdown prose is the note format (no front-matter), closing the format-stability question by observing unparsed text has nothing to destabilize; extends [ADR-0002](../03-adrs/ADR-0002-persistence-content-addressing-and-on-disk-layout.md)'s `.foundry/acts/` commit-to-git convention to `.foundry/knowledge/` by identical reasoning; and explicitly declines Derived-Knowledge indexing, semantic retrieval, provenance scoring, and formal note curation until each has a named, concrete trigger. Status: Proposed, not yet ratified.
- **2026-07-21** — Ratified [ADR-0007](../03-adrs/ADR-0007-knowledge-and-semantic-store.md) under [ADR-0000](../03-adrs/ADR-0000-governance-and-ratification-process.md), the same day it was drafted. [knowledge.md](../02-architecture/knowledge.md)'s "Unresolved: format stability" section resolved (cites this ADR); [getting-started.md](../04-guides/getting-started.md) gains a commit-`.foundry/knowledge/`-to-git recommendation mirroring its existing `.foundry/acts/` one; [roadmap.md](roadmap.md) open decision 7 marked RESOLVED; [invariants.md](../05-reference/invariants.md)'s closing note no longer lists I11 as pending; [README.md](../03-adrs/README.md)'s backlog table updated (row moved to Accepted); [RFC-0004](../01-rfcs/RFC-0004-multi-executor-router-and-publish-policy.md) §4 and [RFC-0005](../01-rfcs/RFC-0005-authored-knowledge-retrieval.md) §4's "Knowledge & semantic store" rows updated to cite this ADR as Accepted. Only "Extension isolation & contract versioning" and "Cost as a first-class constraint" remain in the ADR backlog.
- **2026-07-21** — Shipped a first M5 "change-review UI" slice, code only (no ADR needed — additive, no compatibility surface): (1) `cli.Show`/`formatAct` (`cli/cli.go`) now tints the Verdict line and the patch through `renderVerdict`/`renderDiff` exactly as the live `PromptForApproval` prompt already does — `foundry show` was previously the one Act-review surface without any coloring, and its Verdict line silently dropped the ✓/✗ symbol the live prompt already shows. (2) `formatAct` gains a new "Steps:" section rendering `domain.Act.Steps`' per-Step trace (ID, Kind, a Step-appropriate verdict via the new `cli/render.go`'s `renderStepVerdictLabel`, and duration) — data already recorded by `engine/strategy.go`'s `recordStep` but never surfaced outside live progress narration; a multi-verify Pipeline (e.g. the built-in `review` Pipeline) previously collapsed to only its last verify Step's verdict in `foundry show`'s flat fields, silently dropping an earlier Step's own findings even though the Record already carried them. Both changes are purely additive rendering over already-recorded fields — no new domain/storage fields, no schema change. Tests: `cli/format_test.go`'s `TestFormatAct_ColorTintsVerdictAndPatch` and `TestFormatAct_StepsTrace` (new); `TestFormatAct_Golden`/`TestFormatAct_UnapprovedWithoutEvidenceOrPatch`/`cli_test.go`'s `TestCLI_Show_PrintsFullAct` updated to match. Observability (structured logging/telemetry, M5's other named gap) deliberately not attempted in this pass — no concrete consumer exists yet, and this project's own ADR-0009 already declined a machine-readable output contract for the same "no consumer, no schema" reason; revisit against a real need, not speculatively.
- **2026-07-21** — Shipped a first M5 "observability" slice, code only (no ADR needed — additive, opt-in, no compatibility surface): reconsidered the prior entry's deferral once roadmap.md itself was re-read closely — unlike Derived Knowledge/semantic retrieval (ADR-0007, genuinely no named consumer), M5's own milestone definition names observability directly, which is itself the concrete, named consumer ADR-0009's "no consumer, no schema" reasoning was checking for; ADR-0009 declined a machine-readable *output* contract for `show`/`log`/`do` specifically, a different surface than a side-channel diagnostic log, so no conflict. New `engine.SlogReporter` (`engine/slog_reporter.go`) implements `engine.Reporter` by emitting structured JSON events (`act.gather.start`, `act.execute.start`, `act.verify.start`, `act.verify.done` with verdict/findings-count, `act.repair.start`, `act.repair.skipped`/`act.budget.exceeded` at Warn) via the standard library's `log/slog` — no new dependency. New `engine.MultiReporter` (`engine/reporter.go`) fans one Engine run out to more than one Reporter, the same small composition seam already established once per port by `ExecutorRegistry`/`Router`, `ApplierRegistry`, and `gatherer.Compose`. New `cli.NewReporter` (`cli/reporter.go`) is the one place both composition roots (`cmd/foundry/commands/do.go`, `session/run_pipeline_command.go`) now call instead of `cli.NewProgressReporter` directly: it returns the unchanged human-only `ProgressReporter` unless the `FOUNDRY_LOG` environment variable is set, in which case it also attaches a `SlogReporter` writing JSON lines to stderr — env var unset (the default) is byte-for-byte unchanged behavior, mirroring how every other optional behavior in this codebase (`.foundry/executors.json`, `.foundry/config.json`) defaults to off. Tests: `engine/reporter_test.go` (`MultiReporter` fan-out, including the zero-Reporters case), `engine/slog_reporter_test.go` (decodes emitted JSON lines, asserts level/msg/fields), `cli/reporter_test.go` (`NewReporter`'s env-gated wiring). Documented in [getting-started.md](../04-guides/getting-started.md)'s new "Structured logging" section. Per-Executor cost/timing telemetry (RFC-0004 §2.7) remains a distinct, larger, un-designed follow-up, not attempted here.
- **2026-07-21** — Drafted [ADR-0011](../03-adrs/ADR-0011-cost-as-a-first-class-constraint.md) (Cost as a first-class constraint), out of the backlog's own listed dependency order (ahead of ADR-0008) since it has real, shipped code to ratify/extend (`engine.CostEstimator`, `engine/budget.go`) where ADR-0008 has none yet (M6 not started, and OQ-005 itself recommends deciding nothing on extension isolation until that milestone begins). Closes [ADR-0005](../03-adrs/ADR-0005-executor-contract-and-capability-model.md) Open Question 1 with a firm "no" (`CostEstimator` stays optional — Claude Code's subprocess structurally has no billing signal, not merely "not yet"); proposes a small, genuinely additive domain change (optional `ActualCostUSD` on `Outcome`/`StepRecord`/`Act`, populated by `executor/openai` from the Chat Completions response's `usage` field it currently discards) for a follow-up implementation PR once ratified; declines to make Budget's hardcoded ceilings configurable or to build cost reconciliation/calibration until each has a named, concrete trigger. Status: Proposed, not yet ratified.
- **2026-07-21** — Ratified [ADR-0011](../03-adrs/ADR-0011-cost-as-a-first-class-constraint.md) under [ADR-0000](../03-adrs/ADR-0000-governance-and-ratification-process.md), the same day it was drafted. [roadmap.md](roadmap.md) open decision 9's cost half marked RESOLVED (its paired "near-term single-user value" half explicitly left open, not conflated); [README.md](../03-adrs/README.md)'s backlog table updated (row moved to Accepted; only "Extension isolation & contract versioning" (ADR-0008) remains). Unlike every other ADR ratified so far, this one requires a real follow-up implementation PR (Migration Strategy steps 1–5: `Outcome.ActualCostUSD`, `StepRecord`/`Act` accumulators, `executor/openai` wiring, `engine/strategy.go` wiring, `cli`/`SlogReporter` surfacing) — tracked as in-progress immediately below rather than claimed as already shipped.
- **2026-07-21** — Shipped [ADR-0011](../03-adrs/ADR-0011-cost-as-a-first-class-constraint.md)'s follow-up implementation PR (all 5 Migration Strategy steps): `domain.Outcome` gained `ActualCostUSD *float64` (reported Evidence only, never a Budget gate); `domain.StepRecord` and `domain.Act` gained matching accumulators, plus `Act.ActualCostCoverage()` (a computed helper, not a stored count, for honest "N of M generate Steps" reporting per Decision 3); `executor/openai.Executor.Execute` now decodes the Chat Completions response's `usage` object (previously received and silently discarded — `chatCompletionResponse.Usage`, `actualCostUSD`) using the same per-model price table `EstimateCostUSD` already reads; `engine/strategy.go`'s generate-Step handling accumulates it onto `act.ActualCostUSD` and threads it through `recordStep`; `engine.Reporter` gained one new additive method, `Executed(iteration int, actualCostUSD *float64)`, fired right after a successful `Execute` — `noopReporter`/`MultiReporter` fan it out, `engine.SlogReporter` emits it as `act.execute.done` (with `actual_cost_usd` only when non-nil), `cli.ProgressReporter` implements it as an intentional no-op (this is reported Evidence for `foundry show`/`FOUNDRY_LOG`, not live human narration); `cli/cli.go`'s `formatAct` renders an "Actual cost" line only when `act.ActualCostUSD != nil`, never a permanent empty placeholder. `engine/strategy.go`'s `lastOutcomeAndJudgment` (used by `Engine.Resume`) also carries `ActualCostUSD` forward from a recorded Step so resume doesn't silently drop it. Tests: `engine/reporter_test.go`, `engine/slog_reporter_test.go` (both extended), `executor/openai/openai_test.go` (`TestExecute_Success` extended plus three new tests), `cli/format_test.go` (two new tests) — all existing `fakeReporter`-asserting tests in `engine/engine_test.go` updated for the new `executed:N:none` event every existing (non-cost-reporting) test now emits. `go build ./...`, `go vet ./...`, and `go test -race ./...` all green; `gofmt -l .` clean.
- **2026-07-21** — Shipped M5's "richer review UI" follow-up named in the previous change-review-UI entry: paging for long diffs/findings. New `cli/pager.go` — `maybePage(out io.Writer, page bool, content string) error` pipes content longer than `pagerThresholdLines` (40) through `$PAGER` (default `less -R`, preserving `renderDiff`'s ANSI color) via an injectable `pagerRunner` package variable, falling back to a direct write on any pager failure so a broken `$PAGER` can never eat output. `page` is an explicit caller-decided bool (`colorEnabled(out)`), matching `renderDiff`/`renderVerdict`'s own established pattern of never re-deriving terminal-ness internally — keeps `maybePage` itself trivially testable via a fake `pagerRunner`, no real terminal or subprocess needed. Wired into `cli.PromptForApproval` (the proposed-patch section only; the verdict/prompt lines print directly) and `cli.CLI.Show`. Tests: `cli/pager_test.go` (6 new tests: threshold boundary, `page=false` never pages, pager invocation args/stdin, `$PAGER` respected, pager-failure fallback, `countLines`). No ADR required — additive CLI behavior, no compatibility surface, consistent with M5's other real-code slices this session (change-review UI, observability, actual-cost reporting). `go build ./...`, `go vet ./...`, `go test -race ./...` all green; `gofmt -l .` clean.
- **2026-07-21** — Drafted [ADR-0008](../03-adrs/ADR-0008-extension-isolation-and-contract-versioning.md) (Extension isolation & contract versioning), the last remaining ADR backlog row. Decides no isolation mechanism (in-process/out-of-process/sandboxed/tiered — all rejected as premature, no real third-party extension exists to design against) and no versioning policy (no concrete contract shape exists yet to version) — closes [OQ-005](../06-open-questions/OQ-005-extension-isolation.md) with its own "decide nothing yet" recommendation rather than overriding it. Its real, load-bearing work: corrects [ADR-0001](../03-adrs/ADR-0001-language-and-toolchain.md) clause 4 (and its downstream Consequences/"Harder"/Future-ADR-Dependencies text), which asserted "the plugin/provider contract is defined in a language-neutral wire format (gRPC/protobuf), decided in ADR-0008" as a **binding** decision — an assertion [extensibility.md](../02-architecture/extensibility.md) and [OQ-005](../06-open-questions/OQ-005-extension-isolation.md) had already withdrawn as premature, but ADR-0001's own text was never updated to match, the same class of live contradiction [ADR-0002](../03-adrs/ADR-0002-persistence-content-addressing-and-on-disk-layout.md) corrected for ADR-0001's SQLite clause. R2 (language-agnostic extension boundary) remains fully binding throughout. Names the concrete trigger for a real future mechanism decision: an actual third party wanting to write a Strategy/Executor/Validator/Context Source outside this repository. Status: Proposed, not yet ratified.
- **2026-07-21** — Ratified [ADR-0008](../03-adrs/ADR-0008-extension-isolation-and-contract-versioning.md) under [ADR-0000](../03-adrs/ADR-0000-governance-and-ratification-process.md), the same day it was drafted — **the ADR backlog is now empty.** [ADR-0001](../03-adrs/ADR-0001-language-and-toolchain.md)'s extension-boundary clause (4) and its downstream Consequences ("ADR-0008... becomes low-risk"), "Harder" (WASM hardening track, in-process sandboxing), Migration Strategy (point 2), and Future ADR Dependencies (point 4) text all corrected in place — none of them asserted a decision this project actually stands behind; [extensibility.md](../02-architecture/extensibility.md)'s "Unresolved" callout resolved (deliberate non-decision, not oversight); [OQ-005](../06-open-questions/OQ-005-extension-isolation.md) marked RESOLVED (both its own file and [open-questions/README.md](../06-open-questions/README.md)'s index); [README.md](../03-adrs/README.md)'s backlog table emptied (row moved to Accepted; ADR-0001's "pending amendment" note fully resolved, both halves). M6's row above updated to note the milestone's one policy question is now closed even though the milestone itself remains code-not-started.
- **2026-07-21** — Started a hardening/robustness pass across already-shipped code (no new architecture, no new ADR — pure error-handling and edge-case work), per the maintainer's own direction once the ADR backlog emptied. First fix: `engine/budget.go`'s `tracker.charge` now refuses a negative `estimateUSD` with a distinct, non-`ErrBudgetExceeded` error instead of silently accepting it. Without the guard, `t.costUSD+estimateUSD > t.budget.MaxCostUSD` evaluates false for a negative-enough estimate, *decrementing* `t.costUSD` and permanently loosening `MaxCostUSD`'s ceiling for every later charge in the same Act — a silent defeat of `domain.Budget`'s own contract ("enforced as a constraint, never merely reported") that a broken third-party `CostEstimator` ([ADR-0005](../03-adrs/ADR-0005-executor-contract-and-capability-model.md)) or `ActualCostUSD` ([ADR-0011](../03-adrs/ADR-0011-cost-as-a-first-class-constraint.md)) implementation could trigger, with no crash and no visible symptom until a later Act ran far over budget. New test: `engine/budget_test.go`'s `TestTracker_NegativeEstimateRefusedNotSilentlyLoosened` (asserts the charge is refused, tracker state is unchanged, and the ceiling still enforces correctly afterward). Surfaced by a targeted hardening survey covering `record/`, `gatherer/`, `workspace/`, `executor/`, and `cli/`; the remaining findings from that survey (non-atomic writes in `record.FileStore`/`record.CheckpointStore`, `record.FileStore.List` failing its whole result on a concurrent in-flight `Write`, `gatherer.NaiveGatherer` reading a full file into memory before its byte budget applies, a worktree-creation error path that skips `git worktree prune`) are real but larger/riskier changes to the durable Record and workspace-isolation mechanisms — queued for later iterations of this same pass, not attempted in one PR. `go build ./...`, `go vet ./...`, `go test -race ./...` all green; `gofmt -l .` clean.
- **2026-07-21** — Second hardening iteration: `record.FileStore.Write` (`record/store.go`) is now atomic. Previously `MkdirAll` + `O_EXCL`-open + `Write` could crash between opening and writing, leaving a permanent zero-length `act.json` — `O_EXCL` then refuses every future `Write` for that ID with `ErrAlreadyExists` (mistaking "exists but corrupt" for "already legitimately recorded"), and `Read`/`List` would hit a JSON decode error forever; the Record (I8's durable audit trail) had no way to recover. Fix: encode, write, and `Sync` to a temp file in the same directory, then publish via `os.Link` (not `os.Rename`) — `Link` fails with `EEXIST` if `act.json` already exists, preserving the exact write-once exclusivity `O_EXCL` gave, while guaranteeing any `act.json` a reader can observe is either absent or fully written, never partial. New `ErrNotFound` sentinel (mirroring `ErrAlreadyExists`) lets `List` distinguish the *benign* race its own directory scan can hit — an Act's directory exists (`MkdirAll` already ran) but `act.json` isn't published yet — from a genuine corruption/permissions problem: `List` now skips the former (every other, fully-written Act still comes back) but still propagates the latter as a real error, matching (for the right reason) `record.CheckpointStore.List`'s existing tolerance for a missing file, without adopting its blanket "swallow any error" posture, which would be unsafe for the durable Record. New tests: `TestFileStore_List_SkipsActDirectoryWithNoActJSONYet`, `TestFileStore_Write_RetriesCleanlyAfterSimulatedCrash` (a stray temp file from a simulated crash no longer blocks a retry), `TestFileStore_Write_LeavesNoStrayTempFile` (confirms normal-path cleanup); `TestFileStore_Read_NotFound` extended to assert `errors.Is(err, ErrNotFound)`. `record.CheckpointStore.Save`'s identical non-atomic-write gap (`record/checkpoint.go`) is a lower-severity version of the same issue (checkpoints are explicitly mutable/recomputable, per ADR-0002, unlike act.json) — queued for a later iteration, not fixed here to keep this PR to one mechanism. `go build ./...`, `go vet ./...`, `go test -race ./...` all green; `gofmt -l .` clean.
- **2026-07-21** — Third hardening iteration: `record.CheckpointStore.Save` (`record/checkpoint.go`) is now atomic, the same write-temp-then-publish pattern just applied to `FileStore.Write` — but publishing via `os.Rename`, not `os.Link`, since a checkpoint is explicitly meant to be overwritable (unlike the write-once Record), and `Rename` atomically replaces an existing destination on POSIX where `Link` would instead fail with `EEXIST`. Previously a crash between opening and writing left a truncated `checkpoint.json` — corrupting the exact mechanism `foundry resume` depends on to recover from a crash, the same failure mode `FileStore.Write` had for the Record itself. New tests: `TestCheckpointStore_Save_RetriesCleanlyAfterSimulatedCrash`, `TestCheckpointStore_Save_LeavesNoStrayTempFile`. All of this session's ADR-0007/ADR-0011/ADR-0008 ratification work and the M5 change-review-UI/observability/actual-cost/paging features were committed in 4 logical commits (`docs(adrs)`, `feat(cli)`, `feat(engine)`, `fix(engine,record)`) before this hardening pass continued. `go build ./...`, `go vet ./...`, `go test -race ./...` all green; `gofmt -l .` clean.
- **2026-07-22** — Fourth hardening iteration (running unsupervised overnight, committing each green iteration per the maintainer's own direction): `gatherer.NaiveGatherer` (`gatherer/gatherer.go`) no longer reads an entire file into memory before its `maxContextBytes` budget applies. New `readBounded(path, limit)` opens the file and reads at most `limit+1` bytes via `io.LimitReader`, replacing all four `os.ReadFile` call sites (the named-file phase, the identifier-fallback phase, the supplementary phase, and `findByIdentifier`'s own directory-walk scan). Previously a large binary/log/database dump a repository happens to track — named directly by an Intent, or encountered by the identifier fallback's walk — was read in full regardless of size, an unbounded memory spike despite `maxContextBytes` (100 KiB) advertising a small cap; `findByIdentifier`'s own `len(content) > maxContextBytes` check (already present) ran only *after* that full read. The bound is applied to the overall budget ceiling, not threaded through each call site's own shrinking `remaining` value — a deliberately smaller, safer fix than fully precise per-call bounding would have required. New tests: `TestReadBounded_NeverReadsPastLimitPlusOne` (a file 50x over a small limit still yields exactly `limit+1` bytes), `TestReadBounded_ReturnsFullContentWhenUnderLimit`, `TestReadBounded_MissingFileReturnsError`; all pre-existing `gatherer` tests (`TestGather_BoundsTotalOutput` et al.) still pass unchanged. `go build ./...`, `go vet ./...`, `go test -race ./...` all green; `gofmt -l .` clean.
- **2026-07-22** — Fifth hardening iteration: `workspace.NewWorkspace`'s (`workspace/workspace.go`) error path now attempts `git worktree prune` after a failed `git worktree add`, mirroring `cleanup()`'s own existing fallback for exactly the scenario its comment already documents — a killed process (or here, a disk-full error partway through populating the worktree) can leave `.git/worktrees/<name>` registered after `os.RemoveAll(tmpDir)` has already removed its directory. `prune` only ever removes a registration whose directory no longer exists, so it is always safe to attempt best-effort, even when `worktree add` failed for an unrelated reason. Deliberately does **not** also delete `branchName` in this path (unlike `cleanup()`'s own full teardown): if `worktree add -b branchName` failed because a branch of that name already existed, that branch predates this call and was not created by it — destroying it in an error-cleanup path would be a real, newly-introduced correctness bug, not a fix; caught and corrected before landing, during this same iteration, by reasoning through the "branch name collision" failure mode specifically. New test: `TestNewWorkspace_FailedWorktreeAddPrunesWithoutTouchingExistingBranch` (pre-creates a same-named branch, triggers the failure, asserts the branch survives and no stray worktree registration remains). `go build ./...`, `go vet ./...`, `go test -race ./...` all green; `gofmt -l .` clean.
