{
  "name": "review",
  "steps": [
    { "id": "generate", "kind": "generate" },
    { "id": "verify", "kind": "verify" },
    { "id": "verify-again", "kind": "verify" }
  ],
  "repair": {
    "max_attempts": 0
  }
}
